---
phase: 03-core-ads-commands-error-mapping
plan: 05
type: execute
wave: 3
depends_on: ["03-01", "03-04"]
files_modified:
  - test/integration/ads_client_test.dart
autonomous: true
requirements: [CMD-01, CMD-02, CMD-03, CMD-04, CMD-05, CMD-06, ERR-01]
must_haves:
  truths:
    - "read-after-write returns the written bytes over a live socket"
    - "readWrite round-trips write-then-read on one key"
    - "writeControl(STOP) then readState observably returns STOP"
    - "readDeviceInfo returns DeviceInfo('Dart ADS Mock', v3.1 build 4024)"
    - "the payload-error magic group surfaces AdsException with code == chosen offset"
    - "the AMS-error magic group surfaces AdsException from the AMS header"
  artifacts:
    - path: "test/integration/ads_client_test.dart"
      provides: "per-command success + both live error-level cases against the mock"
  key_links:
    - from: "ads_client_test.dart"
      to: "startMockServer + AdsClient"
      via: "live loopback socket"
      pattern: "startMockServer"
---

<objective>
Prove every core command and both error levels end-to-end against the extended C++
mock (03-01) using the real `AdsClient` (03-04) over a live loopback socket. This is
the phase's own coverage bar (CONTEXT: integration test per command + at least one
live error-response case at each level).

Purpose: satisfies Success Criteria 1, 2, and 4 (each core command has a passing
integration test) and the live half of ERR-01.
Output: `test/integration/ads_client_test.dart`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-ads-commands-error-mapping/03-CONTEXT.md
@.planning/phases/03-core-ads-commands-error-mapping/03-RESEARCH.md

<interfaces>
<!-- test/support/mock_server.dart: Future<MockServer> startMockServer({List<String> args}); -->
<!--   MockServer.port, MockServer.stop(). Connect to 127.0.0.1:server.port. -->
<!-- test/integration/ams_connection_live_test.dart shows the setup: build AmsConnection with -->
<!--   SocketTransport(), source/target AmsAddr, connect(host, port). -->
<!-- AdsClient(connection, {target, source}) from 03-04; methods read/write/readWrite/readState/ -->
<!--   writeControl/readDeviceInfo. AdsException.code from 03-02. AdsState enum. -->
<!-- Mock fixtures (03-01): store keyed (group,offset); seeded (0xF005,0x123); stateful ReadState -->
<!--   seeded RUN(5); magic groups kErrResultGroup=0xE7700000 (offset -> payload result), -->
<!--   kErrAmsGroup=0xE7700001 (offset -> AMS errorCode). ReadDeviceInfo: name 'Dart ADS Mock', v3.1 build 4024. -->
<!-- Tag the suite @Tags(['integration']). Use per-test long timeouts so failures are provably -->
<!--   command/error results, not timeouts. dart test -N uses single-substring (regex alternation -->
<!--   for multi-match); prefer distinct test names for -N filtering. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-command success integration tests</name>
  <files>test/integration/ads_client_test.dart</files>
  <read_first>
    - test/integration/ams_connection_live_test.dart (live setup + teardown pattern)
    - test/support/mock_server.dart (startMockServer contract)
    - lib/src/client/ads_client.dart (method signatures + typed returns)
  </read_first>
  <action>
    Create `test/integration/ads_client_test.dart` tagged @Tags(['integration']). Start one mock
    per test (or a shared setUpAll with fresh connections — but write-back is connection-scoped,
    so use a fresh connection per write/read pairing to keep isolation). Named tests:
    - `write` then `read`: write bytes at (group, offset), read them back, expect equality (CMD-02, CMD-01).
    - `read_write`: readWrite writes then reads the same key in one round-trip; assert returned bytes (CMD-03).
    - `read_state`: readState() returns AdsStateInfo with adsState == AdsState.run, deviceState == 0 (CMD-04).
    - `write_control`: writeControl(adsState: AdsState.stop) then readState() returns AdsState.stop (CMD-05, stateful).
    - `device_info`: readDeviceInfo() returns DeviceInfo(name 'Dart ADS Mock', version 3, revision 1, build 4024) (CMD-06).
    Each test uses a comfortably-long per-request timeout and tears down the mock + connection.
  </action>
  <verify>
    <automated>dart test test/integration/ads_client_test.dart -t integration</automated>
  </verify>
  <acceptance_criteria>
    - Named tests write, read, read_write, read_state, write_control, device_info exist and pass.
    - read reflects a prior write on the same key (write-back proven).
    - write_control changes state observably via readState (stateful proven).
    - device_info returns the exact mock identity triple.
  </acceptance_criteria>
  <done>All six commands pass live against the mock.</done>
</task>

<task type="auto">
  <name>Task 2: Live both-levels error cases via the magic groups</name>
  <files>test/integration/ads_client_test.dart</files>
  <read_first>
    - test/integration/ads_client_test.dart (Task 1 file)
    - .planning/phases/03-core-ads-commands-error-mapping/03-RESEARCH.md (Magic index-group error fixtures)
  </read_first>
  <action>
    Add two named integration tests to `test/integration/ads_client_test.dart`:
    - `result_error`: read at (indexGroup: 0xE7700000, indexOffset: 0x703) -> expect
      throwsA(isA<AdsException>()) whose code == 0x703 and name == 'ADSERR_DEVICE_INVALIDOFFSET'
      (payload-result level).
    - `ams_error`: read at (indexGroup: 0xE7700001, indexOffset: 0x007) -> expect
      throwsA(isA<AdsException>()) whose code == 0x0007 (GLOBALERR_MISSING_ROUTE), surfaced from
      the AMS header before payload decode (AMS-errorCode level).
    Assert in at least one case that the thrown error is NOT an AdsTimeoutException /
    AdsConnectionException (distinct family, live).
  </action>
  <verify>
    <automated>dart test test/integration/ads_client_test.dart -t integration -N error</automated>
  </verify>
  <acceptance_criteria>
    - result_error and ams_error tests pass against the live mock.
    - Each asserts the AdsException.code matches the injected offset.
    - Distinctness from the transport family asserted live.
  </acceptance_criteria>
  <done>Both ADS error levels surface as AdsException end-to-end over a real socket.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mock socket → AdsClient | live untrusted frames (incl. injected errors) cross into the client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-02 | Info disclosure | live error paths | mitigate | Magic-group fixtures exercise BOTH levels over a real socket, so an error-injection test cannot pass via the payload path alone (research Pitfall 1 warning sign) |
| T-3-07 | DoS | test lifecycle | mitigate | Long per-request timeouts + per-test mock teardown so a failure is a provable error result, never a timeout or orphan process |
| T-3-SC | Tampering | package installs | accept | No package installs this phase (SDK-only); slopcheck N/A |
</threat_model>

<verification>
- `dart test test/integration/ads_client_test.dart -t integration` green (all commands + both error levels).
</verification>

<success_criteria>
Every core command and both ADS error levels pass end-to-end against the extended mock via the
real AdsClient over a live loopback socket.
</success_criteria>

<output>
Create `.planning/phases/03-core-ads-commands-error-mapping/03-05-SUMMARY.md` when done.
</output>
