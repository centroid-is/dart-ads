---
phase: 03-core-ads-commands-error-mapping
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/src/connection/ams_connection.dart
  - lib/src/connection/pending_request.dart
  - test/unit/ams_connection_test.dart
  - test/integration/ams_connection_live_test.dart
autonomous: true
requirements: [ERR-01]
must_haves:
  truths:
    - "request() resolves to both the AMS-header errorCode and the response payload"
    - "the AMS errorCode decoded in _onFrame is no longer discarded"
    - "existing correlation/timeout/fan-out behavior is unchanged"
  artifacts:
    - path: "lib/src/connection/ams_connection.dart"
      provides: "request() returning ({int errorCode, Uint8List payload})"
      contains: "errorCode"
  key_links:
    - from: "_onFrame"
      to: "PendingRequest completer"
      via: "completes with header.errorCode + payload slice"
      pattern: "header\\.errorCode"
---

<objective>
Surface the AMS-header `errorCode` that `AmsConnection.request()` currently drops, so
the `AdsClient` (03-04) can throw at BOTH error levels (AMS header errorCode AND payload
result). Change `request()` to resolve to a record `({int errorCode, Uint8List payload})`,
pass `header.errorCode` through `_onFrame`, and update every in-repo caller in the same
task (verify-ordering rule: a task references only files that exist at its completion).

Purpose: ERR-01's both-levels throw is impossible while the errorCode is discarded. The
mapping itself stays OUT of the connection (transport-pure); only the plumbing changes here.
Output: updated `ams_connection.dart` plus its two existing test callers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-ads-commands-error-mapping/03-RESEARCH.md

<interfaces>
<!-- Current: Future<Uint8List> request(int commandId, Uint8List payload, {Duration? timeout}) -->
<!--   completes with the payload slice only; _onFrame discards header.errorCode. -->
<!-- AmsHeader (ams_header.dart) already decodes errorCode (wire offset 24). -->
<!-- PendingRequest (pending_request.dart) holds completer/timer/expectedCommandId; -->
<!--   its Completer<Uint8List> type parameter must change to the record type. -->
<!-- Callers to update: -->
<!--   test/unit/ams_connection_test.dart (~10 sites: `await f` expecting Uint8List; -->
<!--     buildFrame(...) feeds errorCode 0 by default via the AmsHeader encode path). -->
<!--   test/integration/ams_connection_live_test.dart (r1/r2 then decodeReadDeviceInfoResponse). -->
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surface AMS errorCode from request(), update PendingRequest and _onFrame</name>
  <files>lib/src/connection/ams_connection.dart, lib/src/connection/pending_request.dart</files>
  <read_first>
    - lib/src/connection/ams_connection.dart (request() 138-170, _onFrame 246-278, _failClose 291-320)
    - lib/src/connection/pending_request.dart (Completer type parameter)
    - .planning/phases/03-core-ads-commands-error-mapping/03-RESEARCH.md (The request() errorCode seam section)
  </read_first>
  <action>
    Change `request()` to return `Future<({int errorCode, Uint8List payload})>` (records are
    the house style). Update `PendingRequest.completer` to `Completer<({int errorCode, Uint8List payload})>`.
    In `_onFrame`, complete the pending request with `(errorCode: header.errorCode, payload: <slice>)`
    where the slice is the existing `Uint8List.sublistView(frame, AmsTcpHeader.byteLength + AmsHeader.byteLength)`.
    Preserve all existing behavior: demux-before-lookup, remove-wins claim, dropped-response
    counting, command-mismatch error, timeout, and single-shot disconnect fan-out. The
    connection does NOT import or apply the ADS error table — it only surfaces the raw code.
    Update `_failClose`'s pending completeError to the same completer type (errors carry no
    record, unchanged).
  </action>
  <verify>
    <automated>dart analyze lib/src/connection/ams_connection.dart lib/src/connection/pending_request.dart --fatal-infos</automated>
  </verify>
  <acceptance_criteria>
    - request() returns a record with `errorCode` (int) and `payload` (Uint8List).
    - _onFrame passes header.errorCode into the completed value; no other routing changed.
    - The connection layer imports nothing from protocol/ads_error.dart (mapping stays in the client).
    - `dart analyze` clean.
  </acceptance_criteria>
  <done>errorCode is surfaced from every response; transport semantics unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Update the two existing request() callers</name>
  <files>test/unit/ams_connection_test.dart, test/integration/ams_connection_live_test.dart</files>
  <read_first>
    - test/unit/ams_connection_test.dart (all conn.request(...) sites and their `await f` assertions)
    - test/integration/ams_connection_live_test.dart (r1/r2 usage then decodeReadDeviceInfoResponse)
  </read_first>
  <action>
    Update `test/unit/ams_connection_test.dart`: every `await f` that previously yielded a
    Uint8List now yields the record — read `.payload` for the existing byte assertions, and
    add at least one assertion that `.errorCode == 0` on a normal response so the new field is
    covered. Error/timeout/mismatch expectations (throwsA(...)) are unchanged.

    Update `test/integration/ams_connection_live_test.dart`: `final r1 = await f1;` becomes the
    record; pass `r1.payload` to `decodeReadDeviceInfoResponse`. Keep the reorder/disconnect
    assertions intact.
  </action>
  <verify>
    <automated>dart analyze test/integration/ams_connection_live_test.dart --fatal-infos && dart test test/unit/ams_connection_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - ams_connection_test.dart compiles against the record return and passes.
    - At least one unit assertion checks `.errorCode == 0` on a success response.
    - The live integration test compiles (uses `.payload`); it is not run here (needs the mock — wave 3).
  </acceptance_criteria>
  <done>All in-repo request() callers updated; unit suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inbound AMS frame → request Future | the AMS-header errorCode crosses from wire decode to the caller |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-02 | Info disclosure / Elevation | request() errorCode plumbing | mitigate | Router-level failures (missing route 0x07, port 0x06, timeout 0x745) arrive in the AMS header; surfacing the code lets the client throw instead of reporting success on an empty payload (research Pitfall 1) |
| T-3-05 | Tampering | completion invariant | mitigate | remove-wins claim + single-shot fan-out preserved unchanged; record change is purely additive to the completed value |
| T-3-SC | Tampering | package installs | accept | No package installs this phase (SDK-only); slopcheck N/A |
</threat_model>

<verification>
- `dart analyze` clean on connection files.
- `dart test test/unit/ams_connection_test.dart` green with the record return.
</verification>

<success_criteria>
`request()` resolves to `({int errorCode, Uint8List payload})`, the AMS errorCode is no longer
discarded, all existing callers are updated, and correlation/timeout/fan-out are unchanged.
</success_criteria>

<output>
Create `.planning/phases/03-core-ads-commands-error-mapping/03-03-SUMMARY.md` when done.
</output>
