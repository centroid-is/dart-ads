---
phase: 03-core-ads-commands-error-mapping
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/src/protocol/ads_error.dart
  - lib/src/protocol/constants.dart
  - lib/dart_ads.dart
  - test/unit/golden_parity_test.dart
  - test/unit/ads_error_test.dart
autonomous: true
requirements: [ERR-01]
must_haves:
  truths:
    - "code->name/text lookup returns the AdsDef.h constant name and canonical text for every known code"
    - "0x745 (decimal 1861) resolves to ADSERR_CLIENT_SYNCTIMEOUT"
    - "isDeviceError is true for [0x0700,0x0740) and isClientError is true for [0x0740,0x07FF]"
    - "an unknown code yields a synthetic name and never throws"
    - "AdsState.fromCode maps 0..19 to enum members and any other value to a tolerant fallback"
  artifacts:
    - path: "lib/src/protocol/ads_error.dart"
      provides: "Full ADS error table, AdsException, range helpers"
      exports: ["AdsException"]
      min_lines: 80
    - path: "test/unit/ads_error_test.dart"
      provides: "Error-table lookup, range, 1861, unknown-code, AdsState.fromCode tests"
  key_links:
    - from: "AdsException.fromCode"
      to: "error table map"
      via: "code lookup with synthetic fallback"
      pattern: "AdsException\\.fromCode"
---

<objective>
Create the pure ADS error-mapping assets: a full error-code table transcribed from
the vendored `AdsDef.h`, an `AdsException` typed exception distinct from the
transport/wire families, and a tolerant `AdsState` enum. These live in `protocol/`
(pure — no dart:async/dart:io) and are consumed by the `AdsClient` in 03-04.

Purpose: ERR-01 requires ADS error codes to map to a typed exception separate from
`AdsTimeoutException` / `AdsConnectionException` / `MalformedFrameException`, and
CMD-04 requires a typed `AdsState`.
Output: `lib/src/protocol/ads_error.dart`, an `AdsState` enum in `constants.dart`,
updated barrel exports, and `test/unit/ads_error_test.dart`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-ads-commands-error-mapping/03-CONTEXT.md
@.planning/phases/03-core-ads-commands-error-mapping/03-RESEARCH.md

<interfaces>
<!-- Existing exception families this must stay DISTINCT from: -->
<!--   lib/src/protocol/exceptions.dart -> MalformedFrameException -->
<!--   lib/src/connection/exceptions.dart -> AdsTimeoutException, AdsConnectionException -->
<!-- Existing constants.dart holders: AdsState (abstract final class of int consts, -->
<!--   invalid..reconfig — TO BE CONVERTED to an enum) and AdsError (partial int consts, -->
<!--   TO BE REMOVED and superseded by ads_error.dart). -->
<!-- Barrel lib/dart_ads.dart currently exports AdsState and AdsError from constants.dart. -->
<!-- Full error table + AdsState value list are transcribed VERBATIM in 03-RESEARCH.md -->
<!--   ("ADS Error Table" and "AdsState enum" sections). 1861 == 0x745. Client range has an -->
<!--   intentional gap 0x0749 -> 0x0750 (do NOT invent filler). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ads_error.dart (table + AdsException) and AdsState enum</name>
  <files>lib/src/protocol/ads_error.dart, lib/src/protocol/constants.dart</files>
  <read_first>
    - .planning/phases/03-core-ads-commands-error-mapping/03-RESEARCH.md (ADS Error Table + AdsState enum sections — transcribe verbatim)
    - lib/src/protocol/constants.dart (existing AdsState / AdsError holders to replace)
    - lib/src/protocol/exceptions.dart and lib/src/connection/exceptions.dart (family conventions to match, stay distinct)
  </read_first>
  <behavior>
    - adsErrorName(0x0703) == 'ADSERR_DEVICE_INVALIDOFFSET'; adsErrorText(0x0703) == 'invalid indexOffset'
    - adsErrorName(0x0745) == 'ADSERR_CLIENT_SYNCTIMEOUT' (== decimal 1861)
    - AdsException.fromCode(0x0703).isDeviceError == true; .isClientError == false
    - AdsException.fromCode(0x0745).isClientError == true; .isDeviceError == false
    - AdsException.fromCode(0xABCD).name startsWith 'ADS error 0x' (synthetic, no throw)
    - AdsException.fromCode(0x0703).code == 0x0703 (caller can branch on code)
    - AdsState.fromCode(5) == AdsState.run; AdsState.run.code == 5
    - AdsState.fromCode(9999) == AdsState.unknown (tolerant fallback, no throw)
  </behavior>
  <action>
    Create `lib/src/protocol/ads_error.dart` (pure — import only dart:typed_data if needed,
    no dart:async/dart:io):
    - A `const` map (or switch-based lookup) of code -> (constantName, canonicalText),
      transcribed VERBATIM from 03-RESEARCH.md's error table (global 0x0006-0x001A, router
      0x0506-0x0508, device 0x0700-0x072F, client 0x0740-0x0755). Preserve the 0x0749->0x0750
      gap; do not invent entries.
    - Top-level `String adsErrorName(int code)` and `String adsErrorText(int code)` with a
      synthetic fallback for unknown codes (name like `ADS error 0x{hex}`), so a real PLC's
      unknown code still surfaces a usable message.
    - `class AdsException implements Exception` carrying `int code`, `String name`,
      `String message`; a `factory AdsException.fromCode(int code)` that composes name+text;
      getters `bool get isDeviceError => code >= 0x0700 && code < 0x0740` and
      `bool get isClientError => code >= 0x0740 && code <= 0x07FF`; a `toString()` like
      `AdsException: ADS error 0x0703 (ADSERR_DEVICE_INVALIDOFFSET): invalid indexOffset`.
      This is a NEW family, distinct from MalformedFrameException/AdsTimeout/AdsConnection.

    In `lib/src/protocol/constants.dart`, convert the existing `abstract final class AdsState`
    into an enhanced `enum AdsState` with members invalid(0)..exception(19) per the research
    table PLUS an `unknown(-1)` sentinel, a `final int code`, and a static
    `AdsState fromCode(int code)` that returns the matching member or `unknown` (never throws).
    Remove the now-superseded partial `AdsError` constants from constants.dart (they are only
    exported, not used in logic) — the full table in ads_error.dart replaces them.
  </action>
  <verify>
    <automated>dart analyze lib/src/protocol/ads_error.dart lib/src/protocol/constants.dart</automated>
  </verify>
  <acceptance_criteria>
    - ads_error.dart imports nothing from dart:async or dart:io (stays pure).
    - AdsException is a distinct type (not a subtype of the transport/wire exceptions).
    - AdsState is an enum with a `code` int and a tolerant `fromCode`; members 0..19 + unknown.
    - The old partial `AdsError` class is removed from constants.dart.
    - `dart analyze` is clean.
  </acceptance_criteria>
  <done>Pure error table + AdsException + AdsState enum exist and analyze clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire barrel exports, fix golden test, add error-table unit tests</name>
  <files>lib/dart_ads.dart, test/unit/golden_parity_test.dart, test/unit/ads_error_test.dart</files>
  <read_first>
    - lib/dart_ads.dart (barrel — AdsState/AdsError export lines 78-87)
    - test/unit/golden_parity_test.dart (lines ~115 and ~176 use AdsState.run as an int)
  </read_first>
  <action>
    Update `lib/dart_ads.dart`: export `AdsException`, `adsErrorName`, `adsErrorText` from
    `src/protocol/ads_error.dart`; drop the `AdsError` name from the constants.dart export
    (removed in Task 1); keep exporting `AdsState` (now the enum) from constants.dart.

    Fix `test/unit/golden_parity_test.dart`: the two `AdsState.run` usages now reference the
    enum, so change them to `AdsState.run.code` (the encoder and the raw-int comparison both
    need the int 5).

    Create `test/unit/ads_error_test.dart` covering: name+text lookup for representative
    device and client codes; `0x0745 == 1861` resolving to ADSERR_CLIENT_SYNCTIMEOUT;
    isDeviceError/isClientError range boundaries (0x0700, 0x073F, 0x0740, 0x07FF); the
    0x0749->0x0750 gap (0x074A is unknown-fallback); an unknown code surfacing a synthetic
    name without throwing; AdsException.code round-trips; AdsState.fromCode for 0, 5, 19,
    and an out-of-range value (-> unknown).
  </action>
  <verify>
    <automated>dart test test/unit/ads_error_test.dart test/unit/golden_parity_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Barrel exports AdsException + adsErrorName/adsErrorText and no longer exports AdsError.
    - golden_parity_test.dart compiles and passes with AdsState.run.code.
    - ads_error_test.dart passes and covers ranges, 1861, gap, unknown fallback, and AdsState.fromCode.
  </acceptance_criteria>
  <done>Public surface exposes the error family; unit tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device response → error mapping | a non-zero code from a response is turned into a typed exception |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-02 | Spoofing / Info | AdsException.fromCode | mitigate | Unknown/hostile codes get a synthetic name + never throw, so a malformed error value cannot crash the mapper or masquerade as success |
| T-3-04 | Tampering | AdsState.fromCode | mitigate | Tolerant fallback (`unknown`) for out-of-range u16 so a hostile state value never throws (research Pitfall 4) |
| T-3-SC | Tampering | package installs | accept | No package installs this phase (SDK-only); slopcheck N/A |
</threat_model>

<verification>
- `dart analyze` clean on the new/changed protocol files.
- `dart test test/unit/ads_error_test.dart test/unit/golden_parity_test.dart` green.
</verification>

<success_criteria>
A pure, distinct `AdsException` family with a full AdsDef.h-sourced table, range helpers, an
unknown-code fallback, and a tolerant `AdsState` enum — all exported and unit-tested.
</success_criteria>

<output>
Create `.planning/phases/03-core-ads-commands-error-mapping/03-02-SUMMARY.md` when done.
</output>
