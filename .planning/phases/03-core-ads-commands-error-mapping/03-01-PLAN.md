---
phase: 03-core-ads-commands-error-mapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test_harness/mock_server.cpp
autonomous: true
requirements: [CMD-01, CMD-02, CMD-03, CMD-04, CMD-05, ERR-01]
must_haves:
  truths:
    - "A Read after a Write to the same (indexGroup, indexOffset) returns the written bytes"
    - "A WriteControl(state) is observable via a later ReadState (stateful)"
    - "A request to the payload-error magic group returns a response whose ADS result == the request indexOffset"
    - "A request to the AMS-error magic group returns a frame whose AMS header errorCode == the request indexOffset"
    - "mock_server --selftest stays byte-identical to the committed golden"
  artifacts:
    - path: "test_harness/mock_server.cpp"
      provides: "Read/Write/ReadWrite data store, stateful ReadState/WriteControl, two magic error-group fixtures, wrapResponse amsError param"
      contains: "case AoEHeader::READ"
  key_links:
    - from: "runServer switch(aoe.cmdId())"
      to: "connection-scoped store + curAdsState"
      via: "per-command handlers"
      pattern: "AoEHeader::(READ|WRITE|READ_STATE|WRITE_CONTROL|READ_WRITE)"
---

<objective>
Extend the C++ mock ADS server so it answers the five remaining core commands
(Read, Write, ReadWrite, ReadState, WriteControl) with deterministic,
connection-scoped semantics, plus two magic-index-group fixtures that inject a
real ADS error at each of the two error levels (payload `result` and AMS-header
`errorCode`). This is the test double every Phase 3 integration and parity test
runs against.

Purpose: the Dart `AdsClient` (03-04) and the integration/parity suites
(03-05, 03-06) need a live server that persists write-back, reflects state
changes, and can emit genuine error frames end-to-end.
Output: an extended `test_harness/mock_server.cpp` whose `--selftest` byte-output
is unchanged, so the Phase 1 golden gate still passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-ads-commands-error-mapping/03-CONTEXT.md
@.planning/phases/03-core-ads-commands-error-mapping/03-RESEARCH.md

<interfaces>
<!-- From third_party/ADS/AdsLib/AmsHeader.h — the vendored framing structs the mock reuses. -->
<!-- AoEHeader command constants (u16): -->
<!--   READ_DEVICE_INFO 0x0001, READ 0x0002, WRITE 0x0003, READ_STATE 0x0004, -->
<!--   WRITE_CONTROL 0x0005, READ_WRITE 0x0009. AMS_RESPONSE 0x0005. -->
<!-- AoEHeader accessors already used in this file: -->
<!--   cmdId(), invokeId(), length(), sourceAddr(), sourcePort(), targetAddr(), targetPort(). -->
<!-- Request ADS payloads (little-endian), starting at inbuf + sizeof(AmsTcpHeader) + sizeof(AoEHeader): -->
<!--   Read  (0x02): group u32, offset u32, length u32 -->
<!--   Write (0x03): group u32, offset u32, length u32, data[length] -->
<!--   ReadWrite (0x09): group u32, offset u32, readLength u32, writeLength u32, writeData[writeLength] -->
<!--   ReadState (0x04): (no payload) -->
<!--   WriteControl (0x05): adsState u16, deviceState u16, length u32, data[length] -->
<!-- Existing helpers in mock_server.cpp: putU16/putU32, wrapResponse(...), sendResponse(...), -->
<!--   kStateFlagsInAms=18 (stateFlags patch precedent). AMS errorCode wire offset within the -->
<!--   32-byte AMS header is 24 (VERIFIED against ams_header.dart line 88). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Data store + stateful ReadState/WriteControl + ReadWrite</name>
  <files>test_harness/mock_server.cpp</files>
  <read_first>
    - test_harness/mock_server.cpp (the runServer switch at lines 404-426, wrapResponse at 115-130, per-connection block at 355)
    - third_party/ADS/AdsLib/AmsHeader.h (AoEHeader constants + accessors, request struct layouts)
    - .planning/phases/03-core-ads-commands-error-mapping/03-RESEARCH.md (Mock data-store design section — the per-command semantics table)
  </read_first>
  <action>
    Add a connection-scoped store declared inside the accept-loop body (near `inbuf`,
    line ~355) so each accepted connection begins clean (research A3): a
    `std::map<std::pair<uint32_t,uint32_t>, std::vector<uint8_t>> store`, plus
    `uint16_t curAdsState = ADSSTATE_RUN` (5) and `uint16_t curDeviceState = 0`.
    Seed one fixture matching the read_req golden key so a pure Read is meaningful
    (per D-store spec: key (0xF005, 0x123)).

    Add little-endian `getU32`/`getU16` readers mirroring the existing `putU32`/`putU16`
    (or memcpy — both dev + CI are little-endian, but write explicit readers per the
    CLAUDE.md endian rule). Read the request ADS payload at
    `inbuf.data() + sizeof(AmsTcpHeader) + sizeof(AoEHeader)`, bounding every field
    read against `tcp.length()` before dereferencing (V5 input validation — a short
    or hostile frame must never overread `inbuf`).

    Extend the `switch (aoe.cmdId())` with cases:
    - READ (0x02): key=(group,offset); data = first `length` bytes of store[key],
      zero-padded if shorter/absent. Response payload: result u32=0, readLength u32=length, data.
    - WRITE (0x03): store[(group,offset)] = incoming writeData. Response: result u32=0.
    - READ_WRITE (0x09): store[key]=writeData then return first readLength bytes of
      store[key]. Response: result u32=0, readLength u32, data.
    - READ_STATE (0x04): result u32=0, adsState u16=curAdsState, deviceState u16=curDeviceState.
    - WRITE_CONTROL (0x05): set curAdsState=req.adsState, curDeviceState=req.deviceState;
      response result u32=0. So WriteControl(STOP) -> ReadState observably returns STOP.

    Build each response payload with putU32/putU16 into a Frame and layer via the
    existing wrapResponse. Do NOT touch buildReadDeviceInfoRes, runSelftest, or the
    golden path. Keep the READ_DEVICE_INFO case unchanged so --selftest stays byte-identical.
    Respect the --delay-ms deferral and --fragment/--coalesce transmission modes for the new
    responses exactly as the existing ReadDeviceInfo case does (route through sendResponse /
    the deferred-first-response path).
  </action>
  <verify>
    <automated>cmake -S test_harness -B test_harness/build && cmake --build test_harness/build && test_harness/build/mock_server --selftest</automated>
  </verify>
  <acceptance_criteria>
    - The build compiles with no warnings-as-errors regressions.
    - `mock_server --selftest` prints `OK` and exits 0 (ReadDeviceInfo byte-identical to golden).
    - New cases exist for READ, WRITE, READ_WRITE, READ_STATE, WRITE_CONTROL.
    - Store and state variables are declared inside the per-connection block (not file/global scope).
    - Every request-payload field read is bounds-checked against tcp.length() before use.
  </acceptance_criteria>
  <done>The mock answers all six core commands; write-back and stateful WriteControl work; selftest byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: Two magic-index-group error fixtures (both levels) via wrapResponse amsError param</name>
  <files>test_harness/mock_server.cpp</files>
  <read_first>
    - test_harness/mock_server.cpp (wrapResponse at 115-130 — the stateFlags-patch precedent)
    - .planning/phases/03-core-ads-commands-error-mapping/03-RESEARCH.md (Magic index-group error fixtures section)
  </read_first>
  <action>
    Add a defaulted `uint32_t amsError = 0` parameter to `wrapResponse`. After layering,
    when amsError != 0, patch the 4 bytes at `sizeof(AmsTcpHeader) + 24` (AMS header
    errorCode wire offset — VERIFIED) in little-endian, mirroring exactly how the function
    already patches the stateFlags bytes at offset 18. The existing ReadDeviceInfo call
    passes no amsError, so its behavior — and the golden/selftest — is unchanged.

    Define two reserved sentinel index groups as named constants (research A2, settled):
    `kErrResultGroup = 0xE7700000u` and `kErrAmsGroup = 0xE7700001u`. Before the normal
    per-command dispatch, intercept any request whose indexGroup equals one of these:
    - kErrResultGroup: return a response whose ADS `result` u32 == the request's indexOffset
      (readLength=0, empty data for Read/ReadWrite shapes). Any error code becomes testable
      by choosing the offset.
    - kErrAmsGroup: return a response and set the AMS-header errorCode (via the new amsError
      param) == the request's indexOffset; payload result stays 0.

    Document both sentinel constants and the offset->code trick in a comment for the Phase 9
    parity audit and for the Dart tests in 03-05.
  </action>
  <verify>
    <automated>cmake --build test_harness/build && test_harness/build/mock_server --selftest</automated>
  </verify>
  <acceptance_criteria>
    - `wrapResponse` has a defaulted `amsError = 0` param; existing call sites are unchanged.
    - `mock_server --selftest` still prints `OK` (defaulted param leaves ReadDeviceInfo byte-identical).
    - kErrResultGroup and kErrAmsGroup are named constants set to 0xE7700000 / 0xE7700001.
    - A request to kErrResultGroup encodes the offset into the payload result u32.
    - A request to kErrAmsGroup encodes the offset into the AMS header errorCode at wire offset 24.
  </acceptance_criteria>
  <done>Both error levels are injectable end-to-end via magic groups; selftest unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-client → mock recv loop | inbound ADS request bytes are parsed for group/offset/length |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-03 | Tampering / DoS | mock request-payload readers | mitigate | Bounds-check every field read against `tcp.length()` before dereferencing `inbuf`; existing 4 MiB `kMaxFrameBytes` guard remains |
| T-3-01 | Repudiation / determinism | connection-scoped store | mitigate | Store declared inside the per-connection block so no state leaks across tests (research Pitfall 3) |
| T-3-SC | Tampering | package installs | accept | No package installs this phase (C++ uses the pinned vendored AdsLib only); slopcheck N/A |
</threat_model>

<verification>
- `cmake --build test_harness/build` succeeds.
- `test_harness/build/mock_server --selftest` prints `OK` (byte-identical golden).
- Read/Write/ReadWrite/ReadState/WriteControl cases and both magic-group fixtures are present.
</verification>

<success_criteria>
Mock answers all six core commands with write-back + stateful state, injects errors at both
levels via two magic groups, and keeps `--selftest` byte-identical to the committed golden.
</success_criteria>

<output>
Create `.planning/phases/03-core-ads-commands-error-mapping/03-01-SUMMARY.md` when done.
</output>
