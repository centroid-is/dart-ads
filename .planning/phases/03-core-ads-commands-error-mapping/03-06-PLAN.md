---
phase: 03-core-ads-commands-error-mapping
plan: 06
type: execute
wave: 3
depends_on: ["03-01", "03-04"]
files_modified:
  - test/integration/ads_parity_test.dart
autonomous: true
requirements: [TEST-05, CMD-01, CMD-02, CMD-03, CMD-04, CMD-05, CMD-06]
must_haves:
  truths:
    - "each Phase-3-applicable C++ AdsLibTest scenario has a named Dart counterpart"
    - "a large-buffer read (8192 bytes) round-trips correctly"
    - "a request with no reply times out as AdsTimeoutException (parity for testAdsTimeout)"
    - "many concurrent read/write futures each resolve correctly (parity for testParallelReadAndWrite)"
  artifacts:
    - path: "test/integration/ads_parity_test.dart"
      provides: "Dart ports of testAdsReadReqEx2, ...LargeBuffer, ReadDeviceInfo, ReadState, ReadWrite, Write, WriteControl, Timeout, LargeFrames, ParallelReadAndWrite"
      contains: "testAdsReadReqEx2"
  key_links:
    - from: "ads_parity_test.dart group names"
      to: "AdsLibTest/main.cpp scenario names"
      via: "1:1 named counterparts for the Phase 9 audit"
      pattern: "testAds"
---

<objective>
Port the Phase-3-applicable Beckhoff C++ `AdsLibTest/main.cpp` scenarios to Dart against
the extended mock, adapted from ADS port-handle semantics to the Dart connection lifecycle.
Each Dart test group is named 1:1 after its C++ scenario so the Phase 9 parity audit
(TEST-05) can confirm coverage mechanically.

Purpose: the standing User Directive requires each feature phase to port the applicable
AdsLibTest scenarios (partial TEST-05). Phase 3 covers the core-command + timeout + large
frame + parallel scenarios.
Output: `test/integration/ads_parity_test.dart`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-ads-commands-error-mapping/03-VALIDATION.md
@third_party/ADS/AdsLibTest/main.cpp

<interfaces>
<!-- Source scenarios (third_party/ADS/AdsLibTest/main.cpp, struct TestAds / TestAdsPerformance): -->
<!--   testAdsReadReqEx2 (L333): write then NUM_TEST_LOOPS(10) reads, bytesRead==sizeof, value matches -->
<!--   testAdsReadReqEx2LargeBuffer (L427): read 8192-byte buffer, bytesRead==8192 -->
<!--   testAdsReadDeviceInfoReqEx (L456): loop; version/revision/build/name asserts -->
<!--   testAdsReadStateReqEx (L504): adsState==ADSSTATE_RUN, devState==0 -->
<!--   testAdsReadWriteReqEx2 (L548): write+read loop, outBuffer==buffer, ~outBuffer each iter -->
<!--   testAdsWriteReqEx (L698): write then read loop, outBuffer==buffer -->
<!--   testAdsWriteControlReqEx (L778): WriteControl STOP/RUN loop; ReadState reflects each -->
<!--   testAdsTimeout (L943): (C++ is get/set-timeout config) -> Dart: a request that never gets a reply -->
<!--   testLargeFrames (L992): C++ is an UNIMPLEMENTED stub (asserts false) -> Dart implements a real large-frame round-trip -->
<!--   testParallelReadAndWrite (L1020): 96 threads x 1024 reads -> Dart: many concurrent futures -->
<!-- Adaptation rules: C++ port-handle error cases (PORTNOTOPEN, NOAMSADDR) map to Dart connection -->
<!--   lifecycle (not-connected -> AdsConnectionException); unknown-AmsAddr / MISSING_ROUTE is a -->
<!--   Phase 4 (router) concern -> N/A here; invalid group/offset (SRVNOTSUPP 0x701) is injectable -->
<!--   via kErrResultGroup (0xE7700000) with offset 0x701. Port open/close parity = covered-by-equivalent -->
<!--   (Phase 2 ams_connection_live_test connect/close). Document these in a header comment. -->
<!-- AdsClient(connection, {target, source}); startMockServer from test/support/mock_server.dart; -->
<!--   unknown commandId via connection.request(...) times out (mock ignores unknown cmds). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port the seven core-command AdsLibTest scenarios</name>
  <files>test/integration/ads_parity_test.dart</files>
  <read_first>
    - third_party/ADS/AdsLibTest/main.cpp (struct TestAds — the seven core scenarios and exactly what each asserts)
    - .planning/phases/03-core-ads-commands-error-mapping/03-VALIDATION.md (C++ Test-Parity Targets list)
    - lib/src/client/ads_client.dart (method signatures)
    - test/support/mock_server.dart (startMockServer)
  </read_first>
  <action>
    Create `test/integration/ads_parity_test.dart` tagged @Tags(['integration']) with a header
    comment mapping each Dart group to its C++ scenario + noting the adaptation rules (port
    handles -> connection lifecycle; MISSING_ROUTE -> Phase 4 N/A; open/close covered-by-equivalent).
    Add one `group(...)` per scenario, named EXACTLY after the C++ method for the audit:
    - 'testAdsReadReqEx2': write 4 bytes at a key, then read it back 10 times (NUM_TEST_LOOPS),
      assert length + value each iteration; plus an invalid-group case via kErrResultGroup/0x701
      asserting AdsException(code 0x701, SRVNOTSUPP).
    - 'testAdsReadReqEx2LargeBuffer': write 8192 bytes, read 8192 back, assert length == 8192 and bytes match.
    - 'testAdsReadDeviceInfoReqEx': loop readDeviceInfo, assert version 3 / revision 1 / build 4024 / name 'Dart ADS Mock'.
    - 'testAdsReadStateReqEx': readState -> AdsState.run, deviceState 0.
    - 'testAdsReadWriteReqEx2': readWrite writes then reads the same key; loop with a value that
      flips each iteration (mirror ~outBuffer), assert returned bytes match the written bytes.
    - 'testAdsWriteReqEx': write then read loop, assert read == written, flipping the value each iteration.
    - 'testAdsWriteControlReqEx': loop writeControl(STOP)->readState==STOP, writeControl(RUN)->readState==RUN.
    Use long per-request timeouts; fresh connection per group for store isolation; tear down mocks.
  </action>
  <verify>
    <automated>dart test test/integration/ads_parity_test.dart -t integration</automated>
  </verify>
  <acceptance_criteria>
    - Seven groups named exactly testAdsReadReqEx2, testAdsReadReqEx2LargeBuffer, testAdsReadDeviceInfoReqEx, testAdsReadStateReqEx, testAdsReadWriteReqEx2, testAdsWriteReqEx, testAdsWriteControlReqEx.
    - Large-buffer group proves an 8192-byte round-trip.
    - Header comment documents the C++->Dart adaptation + covered-by-equivalent notes.
    - All groups pass against the live mock.
  </acceptance_criteria>
  <done>The seven core-command C++ scenarios have passing named Dart counterparts.</done>
</task>

<task type="auto">
  <name>Task 2: Port testAdsTimeout, testLargeFrames, testParallelReadAndWrite</name>
  <files>test/integration/ads_parity_test.dart</files>
  <read_first>
    - third_party/ADS/AdsLibTest/main.cpp (testAdsTimeout L943, testLargeFrames L992, testParallelReadAndWrite L1020)
    - lib/src/connection/ams_connection.dart (request() timeout override + record return)
    - test/integration/ads_parity_test.dart (Task 1 file)
  </read_first>
  <action>
    Add three more named groups to `test/integration/ads_parity_test.dart`:
    - 'testAdsTimeout': issue a request the mock never answers (an unknown commandId via
      connection.request, which the mock silently ignores) with a short per-request timeout;
      assert throwsA(isA<AdsTimeoutException>()). Document that the C++ original tests
      get/set-timeout config, adapted here to a real timeout firing (Dart's timeout model).
    - 'testLargeFrames': the C++ scenario is an unimplemented stub (asserts false); implement a
      genuine large-frame round-trip — write a large payload (e.g. 64 KiB) and read it back,
      asserting integrity — noting in a comment this exceeds the C++ stub.
    - 'testParallelReadAndWrite': issue many concurrent read/write futures (e.g. 100 pipelined,
      not awaited between issue) via Future.wait; assert every result is correct, proving invoke-ID
      correlation under load (the Dart analogue of the C++ thread fan-out).
    Keep long timeouts (except the deliberate short one in testAdsTimeout) and mock teardown.
  </action>
  <verify>
    <automated>dart test test/integration/ads_parity_test.dart -t integration</automated>
  </verify>
  <acceptance_criteria>
    - Three groups named exactly testAdsTimeout, testLargeFrames, testParallelReadAndWrite.
    - testAdsTimeout asserts AdsTimeoutException from an unanswered request with a short timeout.
    - testLargeFrames round-trips a >=64 KiB payload with integrity; comment notes it exceeds the C++ stub.
    - testParallelReadAndWrite proves many concurrent operations each resolve correctly.
    - The full file passes; all ten Phase-3 parity scenarios are present and green.
  </acceptance_criteria>
  <done>All ten Phase-3-applicable AdsLibTest scenarios have passing named Dart counterparts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mock socket → AdsClient (under load) | many concurrent live frames cross into the client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-05 | Tampering / DoS | concurrent correlation | mitigate | testParallelReadAndWrite proves the invoke-ID map never crosses responses under load; large-frame test exercises the 4 MiB assembler guard boundary well below the cap |
| T-3-07 | DoS | timeout parity | mitigate | testAdsTimeout uses a bounded short timeout against an unanswered request so it terminates deterministically (no hung Future) |
| T-3-SC | Tampering | package installs | accept | No package installs this phase (SDK-only); slopcheck N/A |
</threat_model>

<verification>
- `dart test test/integration/ads_parity_test.dart -t integration` green — all ten named scenarios pass.
</verification>

<success_criteria>
Every Phase-3-applicable C++ AdsLibTest scenario (ten) has a named, passing Dart counterpart
against the mock, with adaptations documented for the Phase 9 parity audit.
</success_criteria>

<output>
Create `.planning/phases/03-core-ads-commands-error-mapping/03-06-SUMMARY.md` when done.
</output>
