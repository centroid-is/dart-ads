---
phase: 02-tcp-transport-connection-lifecycle-invoke-id-correlation
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - test/integration/socket_transport_test.dart
  - test/integration/ams_connection_live_test.dart
autonomous: true
requirements: [TRANS-01, TRANS-03, PROTO-03, TEST-03]
must_haves:
  truths:
    - "A live TCP connection to the mock (ephemeral port via startMockServer) round-trips a real ReadDeviceInfo frame and resolves the request Future"
    - "close() completes done and flips isConnected to false; the mock process tears down cleanly in tearDownAll"
    - "Under mock --delay-ms, two pipelined requests receive out-of-order responses on the wire yet each resolves its own Future with droppedResponses == 0"
    - "Under mock --close-after, a mid-request disconnect fans out: the pending request errors with AdsConnectionException and no Future hangs"
  artifacts:
    - path: "test/integration/socket_transport_test.dart"
      provides: "live connect / ReadDeviceInfo round-trip / clean close (TRANS-01)"
      contains: "startMockServer"
    - path: "test/integration/ams_connection_live_test.dart"
      provides: "live reorder (--delay-ms) + mid-request disconnect (--close-after)"
      contains: "--delay-ms"
  key_links:
    - from: "test/integration/*"
      to: "test/support/mock_server.dart"
      via: "startMockServer(...) + tearDownAll kill"
      pattern: "startMockServer"
    - from: "test/integration/*"
      to: "package:dart_ads/dart_ads.dart"
      via: "AmsConnection + SocketTransport via the public barrel"
      pattern: "AmsConnection"
---

<objective>
Prove the transport + connection stack end-to-end against the live C++ mock over a real socket: a clean open/round-trip/close lifecycle (TRANS-01), invoke-ID correlation under actual on-wire response REORDERING via `--delay-ms` (PROTO-03, success criterion 2's "MUST be proven under reordering"), and disconnect failure fan-out under a mid-request drop via `--close-after` (TRANS-03) — all launched through the shared `startMockServer` helper with an ephemeral port and `LISTENING` handshake (TEST-03).

Purpose: The unit tests (Plan 02-03) prove the logic against `FakeTransport`; this plan proves the same behaviours survive a real `dart:io` Socket, real TCP segmentation, and a real process lifecycle — the phase's live-harness success criteria.

Output: `test/integration/socket_transport_test.dart`, `test/integration/ams_connection_live_test.dart`.

Depends on Plan 02-01 (SocketTransport), 02-02 (mock modes + startMockServer), 02-03 (AmsConnection).

CI note (Claude's discretion per RESEARCH open question 3): the existing Linux `integration` job already runs full `dart test`, which includes these `@Tags(['integration'])` tests — no workflow change is made. This satisfies the locked "extend the integration job to run integration tests" intent without a redundant step. If the Phase gate needs the exact `dart test -t integration` wording, that is a one-line addition, deliberately NOT taken here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-tcp-transport-connection-lifecycle-invoke-id-correlation/02-RESEARCH.md
@.planning/phases/02-tcp-transport-connection-lifecycle-invoke-id-correlation/02-CONTEXT.md
@test/support/hex.dart
@lib/src/protocol/commands.dart

<interfaces>
<!-- Reuse across the three prior plans (all built by Wave 1/2): -->
<!--   startMockServer({List<String> args}) -> Future<MockServer(proc, port)>  -->
<!--     (Plan 02-02) — ephemeral port, LISTENING parse, tearDownAll kill.     -->
<!--   AmsConnection(SocketTransport(), source:, target:, {defaultTimeout});   -->
<!--     await conn.connect('127.0.0.1', server.port);                         -->
<!--     Future<Uint8List> request(commandId, payload, {timeout});            -->
<!--     bool isConnected; Future<void> done; int droppedResponses; close().   -->
<!--   Public barrel exports: AmsConnection, SocketTransport,                  -->
<!--     AdsConnectionException, AdsTimeoutException, AdsCommandId,             -->
<!--     AmsAddr, AmsNetId, decodeReadDeviceInfoResponse.                      -->
<!-- Mock: ReadDeviceInfo (0x01) is the only command; response addressing is  -->
<!--   inverted, invokeId echoed. Payload for ReadDeviceInfo request is empty. -->
<!-- Response payload (after offset 38) decodes via                            -->
<!--   decodeReadDeviceInfoResponse(payload) -> name "Dart ADS Mock".          -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live connect / ReadDeviceInfo round-trip / clean close</name>
  <files>test/integration/socket_transport_test.dart</files>
  <read_first>
    - test/support/mock_server.dart (startMockServer + MockServer teardown contract — Plan 02-02)
    - lib/src/connection/ams_connection.dart (connect/request/close/isConnected/done — Plan 02-03)
    - lib/src/protocol/commands.dart (decodeReadDeviceInfoResponse; ReadDeviceInfo carries an empty payload)
    - .planning/phases/02-tcp-transport-connection-lifecycle-invoke-id-correlation/02-RESEARCH.md (Validation Architecture → TRANS-01 row)
  </read_first>
  <action>
    Create `test/integration/socket_transport_test.dart` with `@Tags(['integration'])` at the top. In `setUpAll`, `startMockServer()` (no extra args → normal mode); in `tearDownAll`, `server.proc.kill(ProcessSignal.sigterm); await server.proc.exitCode;` (or `await server.stop()` if that convenience exists) — guard against orphan processes.

    Tests (name them so `-N` / `-t integration` can select; keep runtime well under the 30s tag timeout):
    - "connects and round-trips ReadDeviceInfo": build `AmsConnection(SocketTransport(), source: AmsAddr(AmsNetId.parse('192.168.0.100.1.1'), 40001), target: AmsAddr(AmsNetId.parse('192.168.0.1.1.1'), AmsPort.plcTc3))`; `await conn.connect('127.0.0.1', server.port)`; expect `conn.isConnected` true; `final payload = await conn.request(AdsCommandId.readDeviceInfo, Uint8List(0));`; `decodeReadDeviceInfoResponse(payload)` and assert `name == 'Dart ADS Mock'` (or at least a non-error decode). Confirms a real frame traversed SocketTransport → FrameAssembler → correlation.
    - "close completes done and flips isConnected": after a round-trip, `await conn.close();` expect `conn.done` completes and `conn.isConnected` is false. Confirms clean teardown (flush+destroy, single-shot fan-out on graceful close).

    Do NOT rely on sleeps for readiness — `startMockServer` already blocks on `LISTENING`.
  </action>
  <verify>
    <automated>dart test test/integration/socket_transport_test.dart -t integration</automated>
  </verify>
  <acceptance_criteria>
    - `dart test test/integration/socket_transport_test.dart -t integration` passes (mock built/launched via the helper; both tests green).
    - `grep -c 'startMockServer' test/integration/socket_transport_test.dart` >= 1 and `grep -Ec 'kill\(|\.stop\(' test/integration/socket_transport_test.dart` >= 1 (clean teardown).
    - `grep -c "@Tags(\['integration'\])" test/integration/socket_transport_test.dart` == 1.
    - `grep -Ec 'isConnected|conn.done|\.done' test/integration/socket_transport_test.dart` >= 1 (lifecycle asserted).
    - `dart format --output=none --set-exit-if-changed test/integration/socket_transport_test.dart` exits 0.
  </acceptance_criteria>
  <done>A live socket round-trip and clean close/teardown are proven against the mock through the shared launch helper (TRANS-01, TEST-03).</done>
</task>

<task type="auto">
  <name>Task 2: Live reorder (--delay-ms) + mid-request disconnect (--close-after)</name>
  <files>test/integration/ams_connection_live_test.dart</files>
  <read_first>
    - test_harness/mock_server.cpp (--delay-ms first-response-deferral + --close-after semantics — Plan 02-02)
    - test/support/mock_server.dart (startMockServer args passthrough)
    - .planning/phases/02-tcp-transport-connection-lifecycle-invoke-id-correlation/02-RESEARCH.md (Reorder correlation test example; Pitfalls 4 & 5 — MUST pipeline; Pattern 3)
  </read_first>
  <action>
    Create `test/integration/ams_connection_live_test.dart` with `@Tags(['integration'])`. Each test starts its OWN mock with the relevant flag and tears it down.

    - "reorder: correlates reordered responses by invoke-ID": `startMockServer(args: ['--delay-ms', '80'])`. Connect. Issue TWO `conn.request(AdsCommandId.readDeviceInfo, Uint8List(0))` calls WITHOUT awaiting between them (capture `f1`, `f2` — they MUST pipeline; awaiting f1 first would leave nothing to reorder, Pitfall 5). The mock defers response #1 and flushes it LAST, so response #2 arrives first on the wire. `await f1; await f2;` — both resolve their own request; assert both decode and `conn.droppedResponses == 0`. This is the on-wire proof of correlation under reordering (success criterion 2).
    - "disconnect: mid-request drop fans out with no hung Future": `startMockServer(args: ['--close-after', '1'])`. Connect. `final f = conn.request(AdsCommandId.readDeviceInfo, Uint8List(0));` — the mock closes the socket on the 1st complete request without answering. Expect `f` to error with `AdsConnectionException` (use `expectLater(f, throwsA(isA<AdsConnectionException>()))`), and expect `conn.done` to complete — proving the pending request fans out and no Future hangs. Bound the test so a hang fails fast (rely on the 30s `integration` tag timeout, but the fan-out should resolve near-immediately).

    Give each request a comfortably-long per-request timeout (or the 5s default) so the reorder test is NOT accidentally satisfied by a timeout firing, and so the disconnect is provably a CONNECTION error, not a timeout.
  </action>
  <verify>
    <automated>dart test test/integration/ams_connection_live_test.dart -t integration</automated>
  </verify>
  <acceptance_criteria>
    - `dart test test/integration/ams_connection_live_test.dart -t integration` passes both tests.
    - `grep -Ec -- "--delay-ms" test/integration/ams_connection_live_test.dart` >= 1 AND `grep -Ec -- "--close-after" test/integration/ams_connection_live_test.dart` >= 1.
    - `grep -Ec 'droppedResponses' test/integration/ams_connection_live_test.dart` >= 1 (reorder asserts no drops).
    - `grep -Ec 'isA<AdsConnectionException>|AdsConnectionException' test/integration/ams_connection_live_test.dart` >= 1 (disconnect asserts the typed connection error, not a timeout).
    - The two `request` calls in the reorder test are NOT awaited between issue (pipelined) — verified by review that both Futures are captured before either is awaited.
  </acceptance_criteria>
  <done>Live reordering correlates correctly with zero dropped responses, and a live mid-request disconnect fans out as AdsConnectionException with no hung Future (PROTO-03, TRANS-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dart test ↔ mock over loopback TCP | Real socket round-trip; the mock is a trusted local test double but exercises the untrusted-peer code paths (reorder, abrupt close) |
| Mock child process lifecycle | Orphaned mock processes would leak ports/handles across the suite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-02 | Denial of Service | live mid-request disconnect (--close-after) | mitigate (verify) | The test asserts the pending request errors `AdsConnectionException` and `done` completes — the on-wire proof that Plan 02-03's single-shot fan-out leaves no hung Future |
| T-2-04 | Spoofing / Tampering | out-of-order wire responses (--delay-ms) | mitigate (verify) | The test asserts each pipelined request resolves its OWN response and `droppedResponses == 0` — the on-wire proof of invoke-ID correlation under reordering |
| T-2-07 | Denial of Service | orphaned mock processes | mitigate | `tearDownAll` (and per-test teardown) `kill(SIGTERM)` + `await exitCode`; `startMockServer` bounds readiness with a timeout that kills on failure |
| T-2-SC | Tampering | package installs | accept | No new packages; tests use only `package:test`, `package:dart_ads`, and Dart SDK libraries |
</threat_model>

<verification>
- `dart test -t integration` runs both integration files green against the freshly-built mock.
- Full `dart test` (unit + integration) green — the phase gate.
- No orphaned `mock_server` processes remain after the suite (teardown asserted).
</verification>

<success_criteria>
- Live open → ReadDeviceInfo round-trip → clean close proven over a real socket via the ephemeral-port launch helper (TRANS-01, TEST-03).
- Live response reordering (`--delay-ms`) correlates by invoke-ID with zero dropped responses (PROTO-03, success criterion 2 proven under reordering).
- Live mid-request disconnect (`--close-after`) fans out as `AdsConnectionException` with no hung Future (TRANS-03).
</success_criteria>

<output>
Create `.planning/phases/02-tcp-transport-connection-lifecycle-invoke-id-correlation/02-04-SUMMARY.md` when done.
</output>
