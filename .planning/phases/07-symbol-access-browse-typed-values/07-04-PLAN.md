---
phase: 07-symbol-access-browse-typed-values
plan: 04
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - test_harness/dump_golden.cpp
  - test/golden/sym_handle_req.hex
  - test/golden/sym_handle_res.hex
  - test/golden/sym_uploadinfo_res.hex
  - test/golden/sym_upload_blob.hex
  - test/unit/symbols_golden_test.dart
autonomous: true
requirements: [SYM-01, SYM-02]
must_haves:
  truths:
    - "dump_golden emits a 0xF003 handle request + 4-byte handle response fixture"
    - "dump_golden emits a SYM_UPLOADINFO response fixture and a 2-symbol upload blob fixture (one padded entry)"
    - "parseSymbolBlob applied to the committed blob golden yields the exact expected AdsSymbolInfo list (byte-for-byte parity)"
  artifacts:
    - path: "test/golden/sym_upload_blob.hex"
      provides: "Reference 2-symbol upload blob"
    - path: "test/unit/symbols_golden_test.dart"
      provides: "Byte-for-byte parity assertions for handle + upload goldens"
      contains: "parseSymbolBlob"
  key_links:
    - from: "test/unit/symbols_golden_test.dart"
      to: "test/golden/sym_upload_blob.hex"
      via: "loadGolden + parseSymbolBlob"
      pattern: "sym_upload_blob"
---

<objective>
Emit the symbol golden fixtures from the C++ dumper (handle req/res, upload-info
response, 2-symbol upload blob with one padded entry) and assert byte-for-byte
Dart-vs-C++ parity through `parseSymbolBlob` (SYM-01 handle, SYM-02 blob).

Purpose: Golden parity pins the wire contract independently of the mock and the
client — the same guarantee every prior codec has.
Output: 4 committed `.hex` fixtures + `test/unit/symbols_golden_test.dart`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-symbol-access-browse-typed-values/07-RESEARCH.md
@lib/src/protocol/symbols.dart

<interfaces>
<!-- Existing golden pipeline. From test_harness/dump_golden.cpp + test/unit/golden_parity_test.dart. -->
- dump_golden already emits req/res .hex vectors (e.g. the 0xF003 ReadWrite req at
  dump_golden.cpp ~265-291 writing 'MAIN.foo'); reuse its emit helper + description style.
- Golden tests load committed .hex via the shared hex-fixture loader used in
  test/unit/golden_parity_test.dart; assert exact byte equality for encode and typed
  values for decode.
- AdsSymbolEntry serialization MUST match the mock's blob (Plan 03) byte-for-byte.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: dump_golden symbol fixtures (handle req/res, uploadinfo, 2-symbol padded blob)</name>
  <files>test_harness/dump_golden.cpp, test/golden/sym_handle_req.hex, test/golden/sym_handle_res.hex, test/golden/sym_uploadinfo_res.hex, test/golden/sym_upload_blob.hex</files>
  <read_first>
    - test_harness/dump_golden.cpp ~46, ~182-291 (emit helper, existing 0xF003/0xF005 examples)
    - 07-RESEARCH.md AdsSymbolEntry layout (must match Plan 03's mock serialization)
  </read_first>
  <action>
    Extend dump_golden to emit: (a) `sym_handle_req.hex` — a 0xF003 ReadWrite request
    (readLen 4, writeData = a symbol name + NUL); (b) `sym_handle_res.hex` — the ReadWrite
    response carrying a 4-byte LE handle; (c) `sym_uploadinfo_res.hex` — a 0xF00C Read
    response of 8 bytes `{nSymbols=2, nSymSize}`; (d) `sym_upload_blob.hex` — a 0xF00B Read
    response whose data is a 2-symbol AdsSymbolEntry blob with ONE entry padded to a
    4-byte boundary (byte-identical to the mock's serialization in Plan 03). Reuse the
    existing emit/description helper. Build and run the dumper to write the four .hex files.
  </action>
  <verify>
    <automated>cd test_harness/build && cmake --build . --target dump_golden 2>&1 | tail -3 && ./dump_golden && ls -1 ../../test/golden/sym_*.hex</automated>
  </verify>
  <acceptance_criteria>
    Four sym_*.hex fixtures generated; blob has 2 entries with one padded entryLength.
  </acceptance_criteria>
  <done>dump_golden emits handle req/res, uploadinfo res, and a 2-symbol padded upload blob.</done>
</task>

<task type="auto">
  <name>Task 2: Byte-for-byte Dart parity test</name>
  <files>test/unit/symbols_golden_test.dart</files>
  <read_first>
    - test/unit/golden_parity_test.dart (hex loader + assertion style)
    - lib/src/protocol/symbols.dart (parseSymbolBlob)
  </read_first>
  <action>
    Create `test/unit/symbols_golden_test.dart`. Load `sym_upload_blob.hex`, slice out the
    ReadWrite/Read response data region, run `parseSymbolBlob(blob, 2)` and assert the two
    AdsSymbolInfo have the exact expected name/typeName/comment/iGroup/iOffs/size/dataTypeId/
    flags — proving the padded entry is handled by entryLength advancement. Load
    `sym_handle_res.hex` and assert the 4-byte handle decodes to the expected u32 LE. Load
    `sym_uploadinfo_res.hex` and assert `{nSymbols, nSymSize}`. Group under names matching
    `-n 'symbol upload golden'` and `-n 'handle golden'`.
  </action>
  <verify>
    <automated>dart test -n 'golden' test/unit/symbols_golden_test.dart</automated>
  </verify>
  <acceptance_criteria>
    Parser output matches the committed blob byte-for-byte; handle + uploadinfo goldens decode as expected.
  </acceptance_criteria>
  <done>`dart test test/unit/symbols_golden_test.dart` green with parseSymbolBlob byte-parity on the padded blob.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| golden fixture → parser | Committed reference bytes; parity failure signals a wire-contract drift, not a runtime attack |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-02 | Tampering | parseSymbolBlob on golden | mitigate | The padded-entry golden proves the parser advances by entryLength (does not trust summed field sizes) against a frozen reference |
| T-7-SC | Tampering | package installs | accept | No package installs this phase |
</threat_model>

<verification>
- `cmake --build . --target dump_golden` + run emits four sym_*.hex
- `dart test test/unit/symbols_golden_test.dart` green
</verification>

<success_criteria>
Committed symbol goldens exist and parseSymbolBlob reproduces them byte-for-byte,
including the deliberately padded entry.
</success_criteria>

<output>
Create `.planning/phases/07-symbol-access-browse-typed-values/07-04-SUMMARY.md` when done
</output>
