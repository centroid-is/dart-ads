---
phase: 07-symbol-access-browse-typed-values
plan: 06
type: execute
wave: 3
depends_on: [07-03, 07-05]
files_modified:
  - test/integration/handle_lifecycle_test.dart
  - test/integration/symbols_test.dart
autonomous: true
requirements: [SYM-01, SYM-02, SYM-03]
must_haves:
  truths:
    - "A live round-trip resolves a handle by name, reads/writes by handle, and releases it against the mock"
    - "N resolve/release cycles return the mock's sym-handle count to baseline (leak proof)"
    - "AdsHandle.close() releases the handle; an unknown name yields 0x710; a released handle reused yields 0x710 and marks the AdsHandle invalid"
    - "browseSymbols returns the mock's 4-symbol table parsed correctly (incl. the padded entry)"
    - "Typed round-trips (DINT, BOOL, STRING, LREAL) read back what was written through the client"
  artifacts:
    - path: "test/integration/handle_lifecycle_test.dart"
      provides: "SYM-01 leak proof + auto-release + staleness"
      contains: "handle count"
    - path: "test/integration/symbols_test.dart"
      provides: "SYM-02 browse + SYM-03 typed round-trips against the mock"
      contains: "browseSymbols"
  key_links:
    - from: "test/integration/handle_lifecycle_test.dart"
      to: "mock sym-handle-count magic group"
      via: "Read baseline → cycles → baseline"
      pattern: "0xE7700003"
---

<objective>
Prove SYM-01/02/03 end-to-end against the C++ mock: handle lifecycle with a leak
proof and staleness, symbol browse of the 4-symbol table (padded entry), and typed
scalar round-trips.

Purpose: The integration layer is where the Dart client, pure codecs, and mock
authority meet — the phase's real acceptance evidence.
Output: `handle_lifecycle_test.dart` + `symbols_test.dart` (tagged integration).

Note: No dedicated C++ AdsLibTest symbol scenario exists — document this in the test
header for the Phase 9 parity audit; do NOT claim TEST-05 coverage here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-symbol-access-browse-typed-values/07-RESEARCH.md
@lib/src/client/ads_client.dart

<interfaces>
<!-- From existing integration tests. -->
- test/integration/ads_client_test.dart shows the `startMockServer` launch helper
  (Process.start + ephemeral port + stdout readiness) + AdsClient construction + teardown.
- @Tags(['integration']) gates these tests; run set is `dart test -x slow`.
- The mock's sym-handle-count magic group (Plan 03, e.g. 0xE7700003) is read via
  `client.read(indexGroup: 0xE7700003, indexOffset: 0, length: 4)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Handle lifecycle integration — leak proof + auto-release + staleness</name>
  <files>test/integration/handle_lifecycle_test.dart</files>
  <read_first>
    - test/integration/ads_client_test.dart (startMockServer + client setup/teardown)
    - lib/src/client/ads_handle.dart (AdsHandle API)
  </read_first>
  <action>
    Create `test/integration/handle_lifecycle_test.dart` (@Tags(['integration'])). Launch
    the mock, build an AdsClient. Tests: (1) resolve MAIN.counter → read DINT → write DINT
    → read-back matches → releaseHandle; (2) leak proof — read the sym-handle-count magic
    group for baseline, run N getHandleByName/releaseHandle cycles, assert the count returns
    to baseline; (3) AdsHandle.create → read/write → close() releases (count back to
    baseline); (4) unknown name throws AdsException code 0x710; (5) reuse of a released
    handle throws 0x710 and, via AdsHandle, marks it invalid (subsequent op throws
    StateError). Put a header comment: "No C++ AdsLibTest symbol scenario exists — flagged
    for the Phase 9 parity audit; this is not TEST-05 coverage."
  </action>
  <verify>
    <automated>dart test -t integration test/integration/handle_lifecycle_test.dart</automated>
  </verify>
  <acceptance_criteria>
    Leak proof returns to baseline; auto-release via close(); unknown name + released-handle → 0x710; staleness → StateError.
  </acceptance_criteria>
  <done>`dart test test/integration/handle_lifecycle_test.dart` green incl. leak proof + staleness.</done>
</task>

<task type="auto">
  <name>Task 2: Browse + typed round-trip integration</name>
  <files>test/integration/symbols_test.dart</files>
  <read_first>
    - test/integration/ads_client_test.dart (setup)
    - lib/src/protocol/symbols.dart + value_codec.dart (expected parsed fields / codec)
  </read_first>
  <action>
    Create `test/integration/symbols_test.dart` (@Tags(['integration'])). Launch the mock,
    build an AdsClient. Tests: (1) browseSymbols returns exactly the 4 mock symbols
    (MAIN.counter/flag/text/temp) with correct name/typeName/iGroup/iOffs/size/dataTypeId —
    proving the padded entry parses; (2) typed round-trips via the client's typed methods:
    write+read DINT (MAIN.counter), BOOL (MAIN.flag), STRING (MAIN.text, incl. a short value
    that NUL-terminates inside the 81-byte buffer), LREAL (MAIN.temp), each asserting the
    read-back equals the written value; (3) SYM-04 — a raw readByHandle returns the
    unparsed Uint8List for MAIN.temp and its bytes match the LREAL encoding.
  </action>
  <verify>
    <automated>dart test -t integration test/integration/symbols_test.dart</automated>
  </verify>
  <acceptance_criteria>
    browseSymbols yields the 4-symbol table; typed round-trips match; raw escape hatch returns unparsed bytes.
  </acceptance_criteria>
  <done>`dart test test/integration/symbols_test.dart` green: browse + typed round-trips + raw hatch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client ↔ mock (live socket) | End-to-end handle + blob exchange; leak and staleness are the observable risks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-01 | DoS | handle lifecycle | mitigate | Leak-proof test asserts the mock's sym-handle count returns to baseline after N cycles + after AdsHandle.close() |
| T-7-05 | Tampering | stale handle reuse | mitigate | Released-handle reuse test asserts 0x710 and AdsHandle invalidation (StateError on next op) |
| T-7-SC | Tampering | package installs | accept | No package installs this phase |
</threat_model>

<verification>
- `dart test -t integration test/integration/handle_lifecycle_test.dart` green
- `dart test -t integration test/integration/symbols_test.dart` green
- Phase gate: `dart test -x slow` full suite green
</verification>

<success_criteria>
End-to-end: handle lifecycle (leak proof + auto-release + staleness), browse of the
4-symbol table (padded entry), and typed round-trips all pass against the mock.
</success_criteria>

<output>
Create `.planning/phases/07-symbol-access-browse-typed-values/07-06-SUMMARY.md` when done
</output>
