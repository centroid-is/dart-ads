---
phase: 04-amsrouter-direct-local-router-transport-modes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/src/protocol/ams_net_id.dart
  - test/unit/protocol/ams_net_id_compare_test.dart
autonomous: true
requirements: [TEST-05, ROUTE-03]
must_haves:
  truths:
    - "AmsNetId values order lexicographically over their 6 bytes (b[0] most significant)"
    - "AmsAddr values order by netId first, then port"
    - "An IPv4 string derives the <ip>.1.1 source NetId with octets in big-endian order"
    - "The testAmsAddrCompare parity assertions all pass"
  artifacts:
    - path: "lib/src/protocol/ams_net_id.dart"
      provides: "Comparable + operator< on AmsNetId/AmsAddr and AmsNetId.fromIpv4"
      contains: "compareTo"
    - path: "test/unit/protocol/ams_net_id_compare_test.dart"
      provides: "1:1-named testAmsAddrCompare parity port + fromIpv4 unit test"
      contains: "testAmsAddrCompare"
  key_links:
    - from: "AmsAddr.compareTo"
      to: "AmsNetId.compareTo then port.compareTo"
      via: "netId-then-port ordering"
      pattern: "netId\\.compareTo"
---

<objective>
Add lexicographic ordering (`Comparable` + `operator<`) to the existing pure
`AmsNetId`/`AmsAddr` value types, plus the `AmsNetId.fromIpv4` factory that
mirrors C++ `AmsNetId(uint32_t)` (`<ip>.1.1`). Port the C++ `testAmsAddrCompare`
scenario 1:1 (Phase-4 slice of TEST-05).

Purpose: ordering is asserted by the C++ parity test and the derivation factory
feeds the router's source-NetId auto-derive (ROUTE-03). Both are pure additions
to `ams_net_id.dart` — no new imports, `protocol/` stays pure.
Output: ordering + fromIpv4 on the value types, and their parity test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-amsrouter-direct-local-router-transport-modes/04-RESEARCH.md

<interfaces>
<!-- Existing value types. Keep operator==/hashCode unchanged; add ordering + factory. -->
From lib/src/protocol/ams_net_id.dart:
  class AmsNetId {                       // 6-byte, Uint8List _bytes, byteLength=6
    factory AmsNetId(List<int> source);
    factory AmsNetId.parse(String dotted);
    Uint8List get bytes; String get dotted;
    // has operator== / hashCode already
  }
  class AmsAddr { AmsAddr(this.netId, this.port); final AmsNetId netId; final int port; }
</interfaces>

<!-- 04-RESEARCH.md transcribes the exact testAmsAddrCompare assertions
     (lines 97-109) and the fromIpv4 octet order (lines 82-85, 246-249).
     Read them there — do NOT re-derive octet order. -->
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ordering and fromIpv4 to AmsNetId/AmsAddr</name>
  <files>lib/src/protocol/ams_net_id.dart</files>
  <read_first>
    - lib/src/protocol/ams_net_id.dart (existing bytes/equality; keep unchanged)
    - 04-RESEARCH.md "Code Examples" (compareTo shape) and Pitfall 1 (octet order)
  </read_first>
  <behavior>
    - AmsNetId implements Comparable<AmsNetId>: compareTo walks the 6 bytes with b[0] most significant; returns -1/0/1. operator< / <= / > / >= derive from compareTo.
    - AmsAddr implements Comparable<AmsAddr>: compareTo compares netId first, then port. operator< derives from compareTo.
    - AmsNetId.fromIpv4("192.168.0.100") => AmsNetId([192,168,0,100,1,1]) (octets big-endian, then 1,1). Throws MalformedFrameException on a non-4-octet or out-of-range input.
    - Existing operator==/hashCode/toString unchanged.
  </behavior>
  <action>
    Implement `Comparable<AmsNetId>` on `AmsNetId` with `compareTo` iterating the
    6 bytes (index 0 most significant) and add `operator<`/`<=`/`>`/`>=` delegating
    to `compareTo`. Implement `Comparable<AmsAddr>` on `AmsAddr` with `compareTo`
    comparing `netId` first then `port`, and add `operator<`. Add a
    `factory AmsNetId.fromIpv4(String dottedIpv4)` that parses the four IPv4
    octets in dotted order (b[0] = first/most-significant octet), appends `1, 1`,
    and reuses the existing 6-byte constructor path (validation + unmodifiable
    copy). Do NOT reverse the octets — little-endian octet order silently
    mis-addresses every direct-mode frame (Pitfall 1). Keep `operator==`/`hashCode`
    exactly as-is. No new imports (the file stays pure).
  </action>
  <verify>
    <automated>dart analyze --fatal-infos lib/src/protocol/ams_net_id.dart</automated>
  </verify>
  <acceptance_criteria>
    - AmsNetId and AmsAddr each implement Comparable with compareTo + operator<.
    - AmsNetId.fromIpv4 exists and validates its input.
    - Analyzer clean under --fatal-infos; equality/hashCode untouched.
  </acceptance_criteria>
  <done>Ordering + fromIpv4 compile clean; existing value-type semantics preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Port testAmsAddrCompare 1:1 + fromIpv4 unit test</name>
  <files>test/unit/protocol/ams_net_id_compare_test.dart</files>
  <read_first>
    - test/integration/ads_parity_test.dart (1:1-named group convention + adaptation header-comment style)
    - 04-RESEARCH.md testAmsAddrCompare transcript (lines 97-109)
  </read_first>
  <action>
    Create `test/unit/protocol/ams_net_id_compare_test.dart` (pure unit, no
    sockets). Add a `group('testAmsAddrCompare', ...)` named EXACTLY after the C++
    method (`third_party/ADS/AdsLibTest/main.cpp` L75-96), with a header comment
    mapping the adaptation (C++ `operator<` → Dart `compareTo`/`operator<`), same
    convention as `ads_parity_test.dart`. Reproduce every assertion against
    `testee = AmsAddr(192.168.0.231.1.1, 1000)`: lower-last-byte < testee,
    lower-middle-byte < testee, same-netId-lower-port < testee, the three
    asymmetry checks `!(testee < x)`, and irreflexivity `!(testee < testee)`. Add
    a second `group` asserting `AmsNetId.fromIpv4("192.168.0.100")` equals
    `AmsNetId([192,168,0,100,1,1])` and that a bad IPv4 throws.
  </action>
  <verify>
    <automated>dart test test/unit/protocol/ams_net_id_compare_test.dart -N "testAmsAddrCompare" && dart analyze --fatal-infos test/unit/protocol/ams_net_id_compare_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `group('testAmsAddrCompare')` exists and all its assertions pass.
    - fromIpv4 derivation + error case covered.
    - Analyzer clean under --fatal-infos.
  </acceptance_criteria>
  <done>testAmsAddrCompare parity port green; fromIpv4 unit-verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → value type | fromIpv4 / ordering take caller-supplied strings; no untrusted wire input here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-02-VAL | Tampering | `AmsNetId.fromIpv4` | mitigate | Reuse the existing 6-byte constructor validation (range + length) so a malformed IPv4 throws `MalformedFrameException` rather than mis-addressing frames |
| T-4-02-ORD | Tampering | ordering octet order | mitigate | Transcribe C++ big-endian octet order exactly; unit-test `192.168.0.100 → 192.168.0.100.1.1` (Pitfall 1) |
| T-4-SC | Tampering | package installs | accept | No package installs in this plan (pure-Dart, no pubspec change) |
</threat_model>

<verification>
- `dart test test/unit/protocol/ams_net_id_compare_test.dart` green.
- `dart analyze --fatal-infos --fatal-warnings` clean.
</verification>

<success_criteria>
- AmsNetId/AmsAddr are Comparable with correct lexicographic + netId-then-port ordering.
- AmsNetId.fromIpv4 derives `<ip>.1.1` with correct octet order.
- testAmsAddrCompare parity port passes (TEST-05 slice).
</success_criteria>

<output>
Create `.planning/phases/04-amsrouter-direct-local-router-transport-modes/04-02-SUMMARY.md` when done.
</output>
