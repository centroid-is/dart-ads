---
phase: 04-amsrouter-direct-local-router-transport-modes
plan: 04
type: execute
wave: 3
depends_on: [04-03]
files_modified:
  - lib/src/router/transport_target.dart
  - lib/src/router/ams_router.dart
  - lib/dart_ads.dart
  - README.md
  - test/integration/router_transport_modes_test.dart
autonomous: true
requirements: [ROUTE-01, ERR-02, TEST-05]
must_haves:
  truths:
    - "A user selects DirectTarget or LocalRouterTarget at runtime without changing command code"
    - "router.connect resolves the route, opens/reuses a connection, and yields an AdsClient with correct source/target addressing"
    - "The same command sequence succeeds through both transport modes against the mock"
    - "A direct-mode missing reverse route surfaces as AdsException code 0x0745/1861 naming the source AmsNetId, never a bare timeout"
    - "Local-router mode leaves the router's own timeout/errors unchanged (no false 0x0745 enrichment)"
  artifacts:
    - path: "lib/src/router/transport_target.dart"
      provides: "sealed TransportTarget → DirectTarget / LocalRouterTarget"
      contains: "sealed class TransportTarget"
    - path: "lib/src/router/ams_router.dart"
      provides: "connect() endpoint selection + DirectTarget ERR-02 timeout enrichment"
      contains: "0x0745"
    - path: "README.md"
      provides: "reverse-route requirement note for direct mode"
      contains: "reverse route"
    - path: "test/integration/router_transport_modes_test.dart"
      provides: "ROUTE-01 dual-mode parity + ERR-02 enriched-1861 integration"
      contains: "ERR02"
  key_links:
    - from: "AmsRouter.connect (DirectTarget)"
      to: "AdsException(0x0745) naming source NetId"
      via: "catch AdsTimeoutException → rethrow enriched"
      pattern: "0x0745|1861"
    - from: "TransportTarget"
      to: "connection endpoint (host/port)"
      via: "DirectTarget deviceHost vs LocalRouterTarget 127.0.0.1"
      pattern: "DirectTarget|LocalRouterTarget"
---

<objective>
Complete Phase 4: the runtime transport-mode selection (`sealed TransportTarget`
→ `DirectTarget` / `LocalRouterTarget`), the `AmsRouter.connect()` wiring that
turns a target NetId into a ready `AdsClient`, and ERR-02 — a direct-mode
missing reverse route surfaced as ADS error 1861/0x745 naming the source NetId
instead of a bare timeout. Prove ROUTE-01 (zero command-code change across modes)
and ERR-02 with integration tests against the C++ mock (which stands in for a
local TwinCAT router unchanged).

Purpose: ROUTE-01 + ERR-02, closing the phase goal.
Output: `transport_target.dart`, `connect()`/ERR-02 in `ams_router.dart`, barrel
exports, README note, and `test/integration/router_transport_modes_test.dart`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-amsrouter-direct-local-router-transport-modes/04-RESEARCH.md
@.planning/phases/04-amsrouter-direct-local-router-transport-modes/04-CONTEXT.md
@.planning/phases/04-amsrouter-direct-local-router-transport-modes/04-03-SUMMARY.md

<interfaces>
<!-- From Plan 03 (this phase). connect()/transport-mode/ERR-02 were deferred to here. -->
From lib/src/router/ams_router.dart:
  class AmsRouter {
    int openPort(); int closePort(int port);
    int addRoute(AmsNetId netId, String host); int removeRoute(AmsNetId netId);
    AmsConnection? getConnection(AmsNetId netId);
    void setLocalAddress(AmsNetId netId); AmsNetId getLocalAddress();
    void resolve(AmsNetId netId);   // throws AdsRoutingException(0x0007) on miss
    // injected connection factory; localAddr auto-derives <ip>.1.1 on first connect
  }
From lib/src/router/routing_exception.dart:
  class AdsRoutingException extends AdsException { ... }  // carries code + named NetId

From lib/src/client/ads_client.dart:
  AdsClient(AmsConnection connection, {required AmsAddr target, required AmsAddr source});
  // the six command methods never touch addressing (ROUTE-01 is structurally true).

From lib/src/connection/exceptions.dart:
  class AdsTimeoutException implements Exception { final int invokeId; final int commandId; }

From lib/src/protocol/ads_error.dart:
  factory AdsException.fromCode(int code);   // 0x0745 = 1861 ADSERR_CLIENT_SYNCTIMEOUT already in table
</interfaces>

<!-- 04-RESEARCH.md "ERR-02 Design" (two-codes table + recommended impl) and
     "Mock / Local-Router Test Staging" (mock inverts addressing, --close-after
     leaves a request unanswered → timeout) are authoritative. -->
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TransportTarget sealed + connect() wiring + ERR-02 enrichment + docs</name>
  <files>lib/src/router/transport_target.dart, lib/src/router/ams_router.dart, lib/dart_ads.dart, README.md</files>
  <read_first>
    - 04-RESEARCH.md "Pattern 1: Sealed TransportTarget strategy" and "ERR-02 Design"
    - lib/src/router/ams_router.dart (Plan 03 registry; connect() deferred to here)
    - lib/src/client/ads_client.dart (constructor + that commands never re-address)
    - lib/src/connection/exceptions.dart (AdsTimeoutException fields)
  </read_first>
  <behavior>
    - DirectTarget(deviceHost, {port=48898}) and LocalRouterTarget({host='127.0.0.1', port=48898}) are the two subtypes of a sealed TransportTarget; host/port configurable so tests point at the mock's ephemeral port.
    - connect(targetNetId, amsPort, {TransportTarget mode}) resolves the route (DirectTarget: route-table host must exist or throw 0x0007; LocalRouterTarget: endpoint is the local router), allocates a local AMS port (30000+) as the SOURCE port, opens/reuses the AmsConnection, and returns an AdsClient with source=AmsAddr(localAddr, allocatedPort), target=AmsAddr(targetNetId, amsPort).
    - In DirectTarget mode ONLY, an AdsTimeoutException from a request is caught and rethrown as an AdsException-family error carrying code 0x0745 (1861) whose message names the source NetId and advises adding a reverse route on the target + checking firewall/port 48898. It stays catchable as AdsException. A non-timeout AdsException propagates UNCHANGED (no masking).
    - In LocalRouterTarget mode, timeouts/errors pass through unchanged (a real router returns 0x0007 itself).
  </behavior>
  <action>
    Create `lib/src/router/transport_target.dart` with a `sealed class
    TransportTarget` and the two subtypes `DirectTarget(String deviceHost,
    {int port = 48898})` and `LocalRouterTarget({String host = '127.0.0.1',
    int port = 48898})` (per 04-RESEARCH Pattern 1). Extend `AmsRouter` with a
    `connect(...)` that selects the endpoint from the `TransportTarget`, allocates
    a local port via `openPort()` (translate the `0` exhaustion sentinel into a
    thrown `AdsException`/`AdsRoutingException` carrying `0x0508`
    ROUTERERR_NOMOREQUEUES), resolves the route (reusing Plan 03 `resolve` for the
    DirectTarget local-table miss → 0x0007 before I/O), opens/reuses the
    `AmsConnection`, and returns an `AdsClient` wired source→target as in the
    behavior block. Wrap the DirectTarget request path so an `AdsTimeoutException`
    is rethrown as an `AdsException`-family error with code `0x0745` and the
    actionable message naming the stamped source NetId (compose from
    `AdsException.fromCode(0x0745)` + router context); let every other exception
    propagate unchanged (ERR-02 must not mask real errors, threat T-4-02). Do NOT
    refactor `AmsConnection` addressing. Export `TransportTarget`, `DirectTarget`,
    `LocalRouterTarget` from `lib/dart_ads.dart` via `show` clauses. Add a
    README/dartdoc note: direct connections require a REVERSE route on the target
    PLC (added via TwinCAT or `adstool addroute`); programmatic AddRoute over
    UDP :48899 is v2 (ROUTE-04).
  </action>
  <verify>
    <automated>dart analyze --fatal-infos lib/src/router/transport_target.dart lib/src/router/ams_router.dart lib/dart_ads.dart</automated>
  </verify>
  <acceptance_criteria>
    - sealed TransportTarget + DirectTarget + LocalRouterTarget exist and are barrel-exported.
    - connect() yields an AdsClient with source=(localAddr, allocated 30000+ port), target=(targetNetId, amsPort).
    - DirectTarget timeout → AdsException code 0x0745 naming source NetId; non-timeout errors unchanged; LocalRouter mode unchanged.
    - README notes the reverse-route requirement. Analyzer clean under --fatal-infos.
  </acceptance_criteria>
  <done>Runtime mode selection + connect() + ERR-02 compile clean; docs updated.</done>
</task>

<task type="auto">
  <name>Task 2: Dual-mode + ERR-02 integration (ROUTE-01, ERR-02, TEST-05 integration slice)</name>
  <files>test/integration/router_transport_modes_test.dart</files>
  <read_first>
    - test/integration/ads_client_test.dart (how clients run command sequences against startMockServer)
    - test/support/mock_server.dart (startMockServer args: --close-after / --delay-ms; MockServer.port / stop())
    - 04-RESEARCH.md "Mock / Local-Router Test Staging" (mock inverts addressing; unanswered request → timeout)
  </read_first>
  <action>
    Create `test/integration/router_transport_modes_test.dart` with
    `@Tags(['integration'])`. ROUTE-01 group: build an `AmsRouter`, run the SAME
    command sequence (e.g. read → write → readState) twice — once with
    `DirectTarget(host:'127.0.0.1', port: server.port)` (route added via
    `addRoute`) and once with `LocalRouterTarget(host:'127.0.0.1',
    port: server.port)` — both against a `startMockServer()`; assert identical
    successful results with ZERO change to the command-level calls (only the
    TransportTarget differs). ERR02 group: point a `DirectTarget` at a mock
    launched so a request is never answered (`--close-after`/`--delay-ms` per the
    Phase-3 `testAdsTimeout` fixture), issue a read, and assert it throws an
    `AdsException` (family) whose `code == 0x0745` and whose message names the
    source AmsNetId — and explicitly assert it is NOT a bare `AdsTimeoutException`.
    Use a short per-request timeout so the test is fast. Tear down every mock via
    `stop()`. Name the ERR02 group so the single `-N` alternation below matches.
  </action>
  <verify>
    <automated>dart test test/integration/router_transport_modes_test.dart -N "ROUTE01|ERR02" && dart analyze --fatal-infos test/integration/router_transport_modes_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Same command sequence passes through DirectTarget AND LocalRouterTarget with no command-code change (ROUTE-01).
    - Direct-mode unanswered request throws AdsException code 0x0745 naming source NetId, never a bare AdsTimeoutException (ERR-02).
    - Single `-N` alternation; analyzer clean under --fatal-infos.
  </acceptance_criteria>
  <done>ROUTE-01 dual-mode parity and ERR-02 enriched-1861 are proven against the mock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → router.connect | mode/host/port are caller config (trusted; no discovery) |
| PLC/router → client | inbound response bytes; correlation + framing guards unchanged from Phase 1-3 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-02 | Repudiation / Information Disclosure | DirectTarget ERR-02 enrichment | mitigate | ONLY `AdsTimeoutException` in DirectTarget mode is enriched to 0x0745; every other exception (device errors, connection errors, malformed frames) propagates unchanged, so a real error is never masked as a route problem. Integration test asserts a non-timeout path stays its own family |
| T-4-01 | Denial of Service | connect() port allocation | mitigate | connect() translates the `openPort()==0` exhaustion sentinel into a typed 0x0508 ROUTERERR_NOMOREQUEUES rather than looping/hanging |
| T-4-03 | Tampering | DirectTarget deviceHost / route host | accept | Endpoint is explicit caller config; no auto-discovery. README documents route hosts as trusted config + reverse-route requirement |
| T-4-05 | Information Disclosure | ERR-02 firewall/port guidance | accept | The message names the caller's own source NetId + operational remediation (firewall/port 48898); not secret, aids the operator |
| T-4-SC | Tampering | package installs | accept | No package installs in this plan (pure-Dart, no pubspec change) |
</threat_model>

<verification>
- `dart test test/integration/router_transport_modes_test.dart` green (ROUTE01 + ERR02 groups).
- Full `dart test` green (all Phase 1-4 unit + integration); mock built via CMake.
- `dart analyze --fatal-infos --fatal-warnings` clean before `/gsd:verify-work`.
</verification>

<success_criteria>
- Direct ↔ local-router transport is runtime-selectable with zero command-code change (ROUTE-01).
- A direct-mode missing reverse route surfaces as ADS 1861/0x745 naming the source NetId, never a bare timeout (ERR-02).
- The router-area TEST-05 slice (parity ports + dual-mode integration) is complete.
</success_criteria>

<output>
Create `.planning/phases/04-amsrouter-direct-local-router-transport-modes/04-04-SUMMARY.md` when done.
</output>
