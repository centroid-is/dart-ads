---
phase: 09-packaging-publishing
plan: "01"
wave: 1
depends_on: []
autonomous: true
requirements: [PKG-01, PKG-02, TEST-05]
files_modified: [pubspec.yaml, .pubignore, example/example.dart, README.md, PARITY.md, .planning/REQUIREMENTS.md]
---

<objective>
Make dart_ads publish-ready: platform declarations, .pubignore, example, README completion, dry-run gate, global-activate gate, and the TEST-05 final C++ parity audit document.
</objective>

<must_haves>
truths:
  - "`dart pub publish --dry-run` exits 0 and the listed archive contains no test_harness/, third_party/, test/, or .planning/ files"
  - "`dart pub global activate --source path .` then `ads --help` exits 0 listing all seven verbs"
  - "PARITY.md maps every AdsLibTest/AdsLibOOITest scenario to a named Dart test or an N/A rationale — no unexplained gaps"
artifacts:
  - pubspec.yaml (platforms, topics, description, version 0.1.0)
  - .pubignore
  - example/example.dart
  - PARITY.md
</must_haves>

<threat_model>
- T-9-01 (info leak): .planning/ or vendored C++ shipping in the artifact — mitigated by .pubignore + dry-run file-list assertion.
- T-9-02 (supply chain): no new deps added this phase.
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: pubspec + .pubignore + example + dry-run gate</name>
  <files>pubspec.yaml, .pubignore, example/example.dart</files>
  <read_first>
    - pubspec.yaml
    - .gitignore
    - lib/dart_ads.dart (public surface for the example)
    - test/integration/ads_client_test.dart (working connect/read patterns to adapt for the example)
  </read_first>
  <action>Update pubspec.yaml: description ("Pure-Dart Beckhoff ADS (AMS/TCP) client — read/write/subscribe PLC variables, symbol browse, sum commands, plus the `ads` CLI. Wire-compatible with the C++ AdsLib, validated byte-for-byte."), version 0.1.0, `topics:` [ads, beckhoff, twincat, plc, industrial-automation], `platforms:` map with linux/macos/windows/android/ios keys (NO web), keep executables. Omit homepage/repository (no remote yet — keeps dry-run clean). Create .pubignore excluding: test/, test_harness/, third_party/, .planning/, CLAUDE.md, dart_test.yaml, .github/, coverage/, PARITY.md is INCLUDED (ship it — it documents test parity; actually exclude it: keep package lean — EXCLUDE PARITY.md too). Create example/example.dart: connect via AmsRouter direct mode, readState, readByName typed DINT, subscribe first 3 notifications, clean close — compiling against the barrel only (no src/ imports); analyzer-clean.</action>
  <verify>
    <automated>dart analyze example --fatal-infos && dart pub publish --dry-run 2>&1 | grep -vE "test_harness/|third_party/|\.planning/|^test/" | tail -5 && ! (dart pub publish --dry-run 2>&1 | grep -E "test_harness/|third_party/|\.planning/") && dart pub publish --dry-run 2>&1 | grep -qE "has 0 warnings|Package has 0 warnings" || dart pub publish --dry-run</automated>
  </verify>
  <done>dry-run exits 0 with no excluded dirs in the file list; example analyzer-clean.</done>
  <acceptance_criteria>
    - pubspec contains platforms: with 5 native keys and no web
    - .pubignore exists excluding test/, test_harness/, third_party/, .planning/
    - dart pub publish --dry-run exit 0
    - example/example.dart imports package:dart_ads/dart_ads.dart only
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: README completion + PARITY.md audit + global-activate gate</name>
  <files>README.md, PARITY.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - README.md
    - .planning/STATE.md (User Directive section — the phase-by-phase parity slice list)
    - third_party/ADS/AdsLibTest/main.cpp (scenario list: grep "void test")
    - test/integration/ads_parity_test.dart, test/unit/router/ams_router_test.dart, test/unit/protocol/ams_net_id_compare_test.dart, test/integration/notification_parity_test.dart, test/integration/ads_notification_test.dart (Dart counterpart names + header notes)
  </read_first>
  <action>Complete README.md: install (pub + global activate), library quickstart (mirroring example), CLI usage for all 7 verbs with flags, limitations (LocalRouterTarget mock-verified only — 0x1000 registration v2; direct mode needs a reverse route on the PLC; no web), test-parity pointer, v2 roadmap (DTYPE-01/02, RECON-01, RPC-01, ROUTE-04, NOTIF-05, TRACE-01). Create PARITY.md: a table of EVERY scenario in AdsLibTest/main.cpp + AdsLibOOITest/main.cpp → Dart test file+group name, or N/A rationale (testConcurrentRoutes: commented out upstream; RingBuffer bytesFree/WriteChunk: C++ internal, covered-by-equivalent FrameAssembler tests; IpV4 testComparsion: C++ helper, equivalent AmsNetId compareTo tests; AdsLibOOITest duplicates: same scenarios OOI-style, covered by same Dart tests; testEndurance: ported, tagged slow). Mark TEST-05 checkbox + traceability Complete in .planning/REQUIREMENTS.md. Run `dart pub global activate --source path .` and `ads --help` as the PKG-02 gate (then `dart pub global deactivate dart_ads` to leave the machine clean).</action>
  <verify>
    <automated>test -f PARITY.md && grep -c "testAds" PARITY.md && dart pub global activate --source path . && ads --help && dart pub global deactivate dart_ads</automated>
  </verify>
  <done>README + PARITY complete; activate gate passed; TEST-05 marked complete.</done>
  <acceptance_criteria>
    - PARITY.md has a row for every "void test..." scenario in both C++ test files
    - ads --help exit 0 after global activate
    - REQUIREMENTS.md TEST-05 checked
  </acceptance_criteria>
</task>
</tasks>

<verification>
Full suite stays green: dart test -x slow. Dry-run + activate gates per tasks.
</verification>
