---
phase: 06-sum-batched-commands
plan: 05
type: execute
wave: 2
depends_on: ["06-01", "06-03"]
files_modified:
  - test/unit/golden_parity_test.dart
autonomous: true
requirements: [SUM-01, SUM-02, SUM-03, SUM-04]

must_haves:
  truths:
    - "The Dart sum builders reproduce the sum_*_req goldens byte-for-byte"
    - "The Dart sum decoders reconstruct the sum_*_res goldens into the expected per-item results"
    - "The sum_read_res golden decodes to item[1] failed + items 0/2 data at correct offsets (SUM-04 golden-pinned)"
    - "The sum_readwrite_res golden decodes using RETURNED lengths (one < requested)"
  artifacts:
    - path: "test/unit/golden_parity_test.dart"
      provides: "Sum golden parity + alignment assertions"
      contains: "sum_read"
  key_links:
    - from: "golden_parity_test sum group"
      to: "test/golden/sum_*.hex"
      via: "readGolden + _adsResponsePayload"
      pattern: "sum_read_res"
---

<objective>
Add byte-for-byte golden parity tests for the three SUMUP commands to `golden_parity_test.dart`: encode the request write-buffers and assert they match `sum_*_req.hex`, and decode `sum_*_res.hex` and assert the per-item `SumResult` list matches the frozen fixtures — including the mid-batch failure alignment (read) and the returned-length slicing (readwrite).

Purpose: This is the plan that makes **goldens + decoder mutually consistent** and freezes the failed-item = 0-data-bytes convention for the Phase 9 audit.
Output: sum parity assertions added to the existing golden parity suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-sum-batched-commands/06-RESEARCH.md
@test/unit/golden_parity_test.dart
@lib/src/protocol/sum_commands.dart

<interfaces>
<!-- From test/unit/golden_parity_test.dart -->
- `readGolden(name)` (via ../support/hex.dart) loads a `.hex` fixture as Uint8List (full on-wire frame).
- `_adsResponsePayload(frame, expectedCommandId)` strips the 6+32 header and returns the trailing ADS payload, asserting wrapper length / command id / response flags / inverted addressing / dataLength.
- Fixture identities: target 192.168.0.1.1.1:851, source 192.168.0.100.1.1:40001, invokeId 1 — matches dump_golden. For a sum RESPONSE the command id is READ_WRITE (0x09); the inner ADS payload is `result u32 + readLength u32 + <sum response region>`.
- Plan 01 signatures: buildSum*Payload -> (Uint8List writeBuffer, int readLength); decodeSum*Response.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sum request-encode + response-decode golden parity</name>
  <files>test/unit/golden_parity_test.dart</files>
  <read_first>test/unit/golden_parity_test.dart (existing read_write group, _adsResponsePayload, readGolden usage, u32le helper); test/golden/sum_*.hex (produced by Plan 03); 06-RESEARCH.md §Wire Layouts + §Golden Fixtures; lib/src/protocol/sum_commands.dart</read_first>
  <action>
Add a `group('sum ...')` block. Construct the exact request item lists that dump_golden baked (same ig/io/len/data values — read them from the committed `sum_*_req.hex` comments / Plan 03 SUMMARY). For each group:

- REQUEST parity: build the inner write-buffer via `buildSum*Payload`, wrap through `encodeReadWriteRequest` (or assert the inner-buffer bytes appear at the correct offset within the golden req frame) with indexGroup=0xF080/81/82, indexOffset=N, and assert the produced frame equals `readGolden('sum_*_req')` byte-for-byte.
- RESPONSE parity: `_adsResponsePayload(readGolden('sum_*_res'), AdsCommandId.readWrite)` → strip the outer `result u32 + readLength u32` → pass the inner sum region to `decodeSum*Response`. Assert the per-item `SumResult` list: for READ, item[1].errorCode != 0 && item[1] has empty data && items[0]/[2] carry the frozen bytes at correct offsets (SUM-04 golden-pinned alignment); for WRITE, all three errorCodes == 0; for READWRITE, the item whose retLen < requested decodes to exactly retLen bytes.
  </action>
  <verify>
    <automated>dart test test/unit/golden_parity_test.dart -n 'sum'</automated>
  </verify>
  <done>All three req goldens reproduce byte-for-byte; all three res goldens decode to the expected per-item results; the read fixture proves mid-batch alignment; the readwrite fixture proves returned-length slicing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| golden fixture bytes → Dart codec | Reference C++ frames decoded by the Dart codec under test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-03 | Information Disclosure | failed-item alignment regression | mitigate | Golden res fixture freezes item[1] failure + items 0/2 offsets; parity test fails on any decoder drift |
| T-6-06 | Tampering | codec/golden divergence | mitigate | Byte-for-byte req encode + res decode parity binds Dart codec to the reference frames |
| T-6-SC | Tampering | package installs | accept | No package installs this phase — RESEARCH Package Legitimacy Audit: N/A |
</threat_model>

<verification>
- `dart test test/unit/golden_parity_test.dart -n 'sum'` green.
</verification>

<success_criteria>
Sum builders/decoders are golden-pinned; the failed-item convention and returned-length rule are frozen by parity assertions.
</success_criteria>

<output>
Create `.planning/phases/06-sum-batched-commands/06-05-SUMMARY.md` when done.
</output>
