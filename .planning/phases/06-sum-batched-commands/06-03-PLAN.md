---
phase: 06-sum-batched-commands
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - test_harness/dump_golden.cpp
  - test/golden/sum_read_req.hex
  - test/golden/sum_read_res.hex
  - test/golden/sum_write_req.hex
  - test/golden/sum_write_res.hex
  - test/golden/sum_readwrite_req.hex
  - test/golden/sum_readwrite_res.hex
autonomous: true
requirements: [SUM-01, SUM-02, SUM-03, SUM-04]

must_haves:
  truths:
    - "dump_golden emits one multi-item (>=3) golden per sum group, req + res"
    - "The sum_read_res golden freezes a mid-batch failure (one item targets kErrResultGroup)"
    - "The sum_readwrite_res golden has at least one returned len < requested len"
  artifacts:
    - path: "test_harness/dump_golden.cpp"
      provides: "Six sum golden emit blocks"
      contains: "sum_read_req"
    - path: "test/golden/sum_read_res.hex"
      provides: "Frozen mid-batch-failure read response"
  key_links:
    - from: "dump_golden.cpp sum emit"
      to: "test/golden/sum_*.hex"
      via: "writeHex"
      pattern: "writeHex"
---

<objective>
Emit six byte-authoritative golden fixtures (req + res for each of 0xF080/81/82) from `dump_golden.cpp`, each a multi-item (>=3) batch, with the READ response freezing a mid-batch failure and the READWRITE response freezing a returned-length < requested-length case. These goldens are the arbiter that pins the Dart codec's failed-item convention (06-RESEARCH A1).

Purpose: Golden parity (Plan 05) asserts the Dart decoders match these frames byte-for-byte, making goldens + decoder mutually consistent. The goldens must encode the SAME failed-item = 0-data-bytes convention as the mock (Plan 02) and decoder (Plan 01).
Output: Extended `dump_golden.cpp` + 6 committed `.hex` fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-sum-batched-commands/06-RESEARCH.md
@test_harness/dump_golden.cpp

<interfaces>
<!-- Golden-emit helpers. From test_harness/dump_golden.cpp. -->
- `writeHex(dir, name, comment, bytes)` -> bool; returns false on I/O error and the caller ANDs into `ok` (line ~112). Any failed write must keep `ok=false` so CI reproducibility gate cannot false-pass.
- `payloadFrame(std::vector<uint8_t>&)` -> Frame; `wrap(Frame, cmdId, isResponse)` builds the full on-wire frame with the fixed fixture addressing (target 192.168.0.1.1.1:851, source 192.168.0.100.1.1:40001, invokeId 1).
- `putU32(vec, val)` appends a u32 LE. Existing ReadWrite block uses `AoEReadWriteReqHeader(group, offset, readLen, writeLen)` prepended before writeData.
- `kErrResultGroup = 0xE7700000u` sentinel (must match mock_server.cpp).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add six sum golden emit blocks to dump_golden.cpp</name>
  <files>test_harness/dump_golden.cpp</files>
  <read_first>test_harness/dump_golden.cpp lines 100-320 (writeHex, ReadWrite req/res block as the closest analog, wrap, putU32); 06-RESEARCH.md §Wire Layouts A/B/C + §Golden Fixtures</read_first>
  <action>
Add six emit blocks mirroring the existing ReadWrite golden pattern. Each is an outer ReadWrite (0x09) frame to the SUMUP group with `indexOffset = N` and the pinned inner write-buffer / response layout. Use N >= 3 items.

- `sum_read_req` / `sum_read_res` (group 0xF080): req inner write-buffer = 3×12B (ig,io,len); readLength = N*4+Σlen. res = 3×u32 errs where item[1]'s inner group is `kErrResultGroup` so its err word is non-zero and it emits 0 data bytes; THEN concatenated data for items 0 and 2 at their requested lengths. Item[1]'s request MUST use ig=kErrResultGroup, io=<a real ADS code e.g. 0x703> so the res err word matches.
- `sum_write_req` / `sum_write_res` (group 0xF081): req = 3×12B headers THEN concatenated distinct write data; readLength = N*4. res = 3×u32 errs (all 0).
- `sum_readwrite_req` / `sum_readwrite_res` (group 0xF082): req = 3×16B (ig,io,rLen,wLen) THEN concatenated write data; readLength = N*8+ΣrLen. res = 3×(err u32, retLen u32) THEN data; make at least one item's retLen < its requested rLen to pin returned-length slicing.

Comment each block with the exact field values (mirror existing golden comments). AND every `writeHex` return into `ok`.
  </action>
  <verify>
    <automated>cmake --build test_harness/build --target dump_golden 2>&1 | tail -5</automated>
  </verify>
  <done>dump_golden compiles; six sum emit blocks present; item[1] of the read fixture targets kErrResultGroup; one readwrite item has retLen < requested.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate + commit the six golden fixtures, verify drift gate</name>
  <files>test/golden/sum_read_req.hex, test/golden/sum_read_res.hex, test/golden/sum_write_req.hex, test/golden/sum_write_res.hex, test/golden/sum_readwrite_req.hex, test/golden/sum_readwrite_res.hex</files>
  <read_first>06-RESEARCH.md §Validation Architecture (golden regen + drift gate: `dump_golden test/golden && git diff --exit-code`)</read_first>
  <action>Run `dump_golden test/golden` to emit the six new `.hex` files. Confirm each file exists and is non-empty with the `# comment` header + hex line format. Re-run once more and confirm `git status` shows the files stable (byte-identical regeneration) — the CI reproducibility gate is `dump_golden && git diff --exit-code`.</action>
  <verify>
    <automated>test_harness/build/dump_golden test/golden && ls test/golden/sum_read_req.hex test/golden/sum_read_res.hex test/golden/sum_write_req.hex test/golden/sum_write_res.hex test/golden/sum_readwrite_req.hex test/golden/sum_readwrite_res.hex && test_harness/build/dump_golden test/golden && git diff --exit-code test/golden/sum_*.hex</automated>
  </verify>
  <done>Six `sum_*.hex` fixtures exist, are byte-stable across re-runs (drift gate clean), and are committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dump_golden → committed goldens | Build-tool output; the CI reproducibility gate guards silent drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-04 | Tampering | silent golden drift / false-pass | mitigate | `ok &=` every writeHex; `dump_golden && git diff --exit-code` reproducibility gate proves byte-stable regeneration |
| T-6-03 | Information Disclosure | frozen failed-item convention | mitigate | sum_read_res encodes item[1] failure as err-word + 0 data bytes, freezing the alignment contract for the decoder |
| T-6-SC | Tampering | package installs | accept | No package installs this phase — RESEARCH Package Legitimacy Audit: N/A |
</threat_model>

<verification>
- `dump_golden` compiles and emits six fixtures.
- Re-run + `git diff --exit-code` clean (reproducible).
</verification>

<success_criteria>
Six multi-item sum goldens exist; the read fixture freezes a mid-batch failure; the readwrite fixture freezes a returned-len < requested-len case; regeneration is byte-stable.
</success_criteria>

<output>
Create `.planning/phases/06-sum-batched-commands/06-03-SUMMARY.md` when done.
</output>
