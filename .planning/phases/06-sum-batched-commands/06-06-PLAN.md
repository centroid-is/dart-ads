---
phase: 06-sum-batched-commands
plan: 06
type: execute
wave: 3
depends_on: ["06-02", "06-04"]
files_modified:
  - test/integration/ads_client_test.dart
autonomous: true
requirements: [SUM-01, SUM-02, SUM-03, SUM-04]

must_haves:
  truths:
    - "Live sumRead/sumWrite/sumReadWrite round-trip against the C++ mock over a real socket"
    - "A read-after-sumWrite proves per-item write-back landed in the mock store"
    - "A mid-batch failure surfaces item k's error while items != k carry correct data (no batch throw)"
    - "A 100-item batch travels in a single frame (Phase-3 concurrency lesson in one request)"
  artifacts:
    - path: "test/integration/ads_client_test.dart"
      provides: "Live sum integration tests"
      contains: "sum"
  key_links:
    - from: "AdsClient.sumWrite"
      to: "AdsClient.read (read-after-write)"
      via: "mock store write-back per item"
      pattern: "sumWrite"
---

<objective>
Add live integration tests that exercise the three sum commands end-to-end against the rebuilt C++ mock (Plan 02): batch read/write/readwrite, read-after-sumWrite write-back, the SUM-04 mid-batch partial failure, and a 100-item single-frame batch. This is the live half proving the mock + client + decoder agree on the frozen convention.

Purpose: Closes SUM-01/02/03/04 on the live path; the mid-batch failure test is the load-bearing SUM-04 litmus against a real socket.
Output: sum integration group added to `test/integration/ads_client_test.dart`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-sum-batched-commands/06-RESEARCH.md
@test/integration/ads_client_test.dart

<interfaces>
<!-- From test/integration/ads_client_test.dart -->
- `startMockServer()` launches the C++ mock (ephemeral port + stdout readiness); teardown registered per test.
- Helper builds a fresh AmsConnection + AdsClient per test; store + ADS-state are connection-scoped (per-test isolation, research Pitfall 3).
- `requestTimeout = Duration(seconds: 10)` so any failure is provably an ADS result, never a timeout (threat T-3-07).
- Magic sentinel `kErrResultGroup == 0xE7700000` — a per-item request targeting this inner group makes the mock inject that item's error (from Plan 02).
- Public API (Plan 04): `client.sumRead/sumWrite/sumReadWrite`, `SumReadRequest/SumWriteRequest/SumReadWriteRequest`, `SumResult<T>{errorCode, value, isSuccess, valueOrThrow}`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live batch read/write/readwrite + read-after-sumWrite</name>
  <files>test/integration/ads_client_test.dart</files>
  <read_first>test/integration/ads_client_test.dart (existing group structure, startMockServer helper, per-test connection setup, requestTimeout); 06-RESEARCH.md §Validation Architecture (Phase Requirements → Test Map)</read_first>
  <action>
Add a `group('sum ...')` block. Tests: (1) sumWrite N items to distinct (group,offset) keys, then sumRead the SAME keys and assert each item's data equals what was written (write-back through the mock store proves SUM-02); (2) sumRead-only of >=3 keys returns per-item success + correct bytes (SUM-01); (3) sumReadWrite of >=3 items returns per-item data at returned lengths (SUM-03). All under `requestTimeout`. Assert every SumResult.isSuccess for the all-success batches.
  </action>
  <verify>
    <automated>dart test test/integration/ads_client_test.dart -n 'sum'</automated>
  </verify>
  <done>Live batch read/write/readwrite pass; read-after-sumWrite proves per-item write-back.</done>
</task>

<task type="auto">
  <name>Task 2: Mid-batch partial failure + 100-item single-frame batch</name>
  <files>test/integration/ads_client_test.dart</files>
  <read_first>test/integration/ads_client_test.dart; 06-RESEARCH.md §Common Pitfalls Pitfall 1 (SUM-04 bug class), §Mock Dispatch (kErrResultGroup per item)</read_first>
  <action>
Add: (1) a SUM-04 partial-failure test — a batch where item k targets `kErrResultGroup` (inner indexOffset = a real ADS code) and the surrounding items target real store keys; assert `results[k].isSuccess == false` with the expected error code, `results[j].isSuccess == true` for j != k with correct data at correct offsets, and that NO exception is thrown for the batch. (2) A 100-item sumRead in ONE call — assert 100 results return and the batch completes in a single round-trip (the Phase-3 concurrency lesson exercised in one frame). Keep both under `requestTimeout`.
  </action>
  <verify>
    <automated>dart test test/integration/ads_client_test.dart -n 'sum.*(partial|large)'</automated>
  </verify>
  <done>Mid-batch failure surfaces item k's error while others carry data with no batch throw; 100-item batch returns 100 results in one frame.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live mock socket → AdsClient | Real response frames (per-item errs + variable data) decoded end-to-end |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-03 | Information Disclosure | live mid-batch alignment | mitigate | Partial-failure integration test asserts items != k carry correct data — proves alignment on the live path, not just goldens |
| T-6-02 | DoS | 100-item batch sizing | mitigate | Mock caps total against kMaxFrameBytes (Plan 02); large-batch test stays well under the 4MiB frame guard |
| T-6-SC | Tampering | package installs | accept | No package installs this phase — RESEARCH Package Legitimacy Audit: N/A |
</threat_model>

<verification>
- `dart test test/integration/ads_client_test.dart -n 'sum'` green (requires the Plan 02 mock rebuilt).
- Full suite `dart test -x slow` green before the phase gate.
</verification>

<success_criteria>
All three sum commands round-trip live; the mid-batch failure litmus (SUM-04) passes against the real mock; a 100-item batch travels in one frame.
</success_criteria>

<output>
Create `.planning/phases/06-sum-batched-commands/06-06-SUMMARY.md` when done.
</output>
