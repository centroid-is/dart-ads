---
phase: 08-dart-cli
plan: 07
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - lib/src/cli/commands/action_command.dart
  - test/integration/cli_action_test.dart
autonomous: true
requirements: [CLI-07]
user_setup: []

must_haves:
  truths:
    - "`action --state RUN` changes device state via WriteControl and prints old -> new state"
    - "State names are accepted case-insensitively (run/RUN/Run) and mapped to AdsState"
    - "An unknown state name exits 2 (usage), never crashes"
  artifacts:
    - path: "lib/src/cli/commands/action_command.dart"
      provides: "action verb over readState + writeControl"
      contains: "writeControl"
    - path: "test/integration/cli_action_test.dart"
      provides: "subprocess state-change proof (old -> new)"
  key_links:
    - from: "lib/src/cli/commands/action_command.dart"
      to: "AdsClient.writeControl"
      via: "AdsState from --state"
      pattern: "writeControl"
    - from: "lib/src/cli/commands/action_command.dart"
      to: "AdsClient.readState"
      via: "read old state before, new state after"
      pattern: "readState"
---

<objective>
Implement the action verb (CLI-07): issue a device state change via WriteControl
selected by `--state=RUN|CONFIG|STOP|...` (case-insensitive, mapped through the
AdsState enum), printing the old -> new state. RPC / method-call mode is explicitly
DEFERRED to v2 (RPC-01) per 08-CONTEXT and is NOT implemented here.

Purpose: action is the control verb and the last of the seven, proving readState +
writeControl from the terminal. Output: one command file + one integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-dart-cli/08-CONTEXT.md
@lib/dart_ads.dart
@test/support/mock_server.dart

<interfaces>
<!-- AdsClient + AdsState (lib/dart_ads.dart) -->
- Future<AdsStateInfo> readState({Duration? timeout});  // AdsStateInfo.adsState is an AdsState
- Future<void> writeControl({required AdsState adsState, int deviceState = 0, Uint8List? data, Duration? timeout});
- AdsState enum: run(5), config(15), stop(6), reset(2), init(3), start(4), idle(1), ...; AdsState.fromCode(int); each has a lower-case name via .name.
<!-- Mock: WriteControl (0x05) stores curAdsState; ReadState (0x04) returns it -> old->new is observable within one connection (mock_server.cpp). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: action verb (--state -> WriteControl, print old -> new)</name>
  <read_first>lib/src/cli/commands/action_command.dart, lib/src/cli/base_command.dart, lib/src/protocol/constants.dart</read_first>
  <files>lib/src/cli/commands/action_command.dart</files>
  <action>
  Fill ActionCommand.run(): flag `--state <name>` (required). Map the name to an
  AdsState case-insensitively by matching against `AdsState.values` `.name`
  (accept e.g. run/RUN/config/stop); an unmatched name throws a UsageException
  (exit 2) listing the valid names — never AdsState.unknown silently. In
  `guarded(...)`: connect, `readState()` for the OLD state, `writeControl(adsState:
  target)`, then `readState()` again for the NEW state, and print
  `"<old.name> -> <new.name>"`. `close()` in finally. Return exitOk. Do NOT
  implement RPC/method-call mode (deferred to v2 RPC-01).
  </action>
  <acceptance_criteria>
  - `action --state RUN` performs WriteControl and prints old -> new; case-insensitive names accepted; unknown name -> exit 2.
  </acceptance_criteria>
  <verify>
    <automated>dart analyze --fatal-infos lib/src/cli/commands/action_command.dart</automated>
  </verify>
  <done>action changes state via WriteControl and reports old -> new; unknown state -> exit 2; no RPC mode.</done>
</task>

<task type="auto">
  <name>Task 2: action state-change integration test</name>
  <read_first>test/support/mock_server.dart, test/integration/cli_contract_test.dart</read_first>
  <files>test/integration/cli_action_test.dart</files>
  <action>
  Create `@Tags(['integration'])` test/integration/cli_action_test.dart reusing the
  runCli/startMockServer pattern. Cases: (1) `action --state CONFIG` exits 0 and
  stdout shows an `<old> -> config` transition (the mock's WriteControl is stateful
  within a connection; the same invocation reads old-state, writes, reads new-state,
  so old->new is observable in one process); (2) `action --state RUN` exits 0 with
  `-> run`; (3) `action --state BOGUS` exits 2. Keep to these cases.
  </action>
  <acceptance_criteria>
  - State changes exit 0 and print old -> new; unknown state -> exit 2.
  </acceptance_criteria>
  <verify>
    <automated>dart test -t integration test/integration/cli_action_test.dart</automated>
  </verify>
  <done>action proven end-to-end: --state changes device state and prints old -> new.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terminal --state -> action | operator-supplied state name untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-10 | Tampering | --state name parsing | mitigate | match against AdsState.values names; unknown -> UsageException (exit 2), never AdsState.unknown/silent no-op |
| T-8-11 | Elevation | unintended RUN/CONFIG/STOP transition | accept | operator-initiated control action; old->new printed for confirmation; RPC mode deferred (smaller surface) |
| T-8-SC | Tampering | pub installs | accept | no new packages beyond 08-01's `args` (Approved) |
</threat_model>

<verification>
- `dart analyze --fatal-infos lib/src/cli/commands/action_command.dart`.
- `dart test -t integration test/integration/cli_action_test.dart` passes.
</verification>

<success_criteria>
- action changes device state via WriteControl (--state, case-insensitive) and prints old -> new.
- Unknown state name maps to exit 2; no RPC/method-call mode (deferred v2).
</success_criteria>

<output>
Create `.planning/phases/08-dart-cli/08-07-SUMMARY.md` when done.
</output>
