---
phase: 01-protocol-framing-codecs-c-golden-frame-harness
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - test_harness/CMakeLists.txt
  - test_harness/dump_golden.cpp
  - test/golden/read_device_info_req.hex
  - test/golden/read_device_info_res.hex
  - test/golden/read_req.hex
  - test/golden/read_res.hex
  - test/golden/write_req.hex
  - test/golden/write_res.hex
  - test/golden/read_state_req.hex
  - test/golden/read_state_res.hex
  - test/golden/write_control_req.hex
  - test/golden/write_control_res.hex
  - test/golden/read_write_req.hex
  - test/golden/read_write_res.hex
autonomous: true
requirements: [TEST-02]
must_haves:
  truths:
    - "A CMake build compiles dump_golden from the vendored AdsLib sources on macOS (dev) with a 3.16 floor"
    - "dump_golden emits byte-accurate reference request AND response frames as committed hex files"
    - "The ReadDeviceInfo request golden equals the RESEARCH-verified 38-byte anchor exactly"
  artifacts:
    - path: "test_harness/CMakeLists.txt"
      provides: "self-owned CMake build listing AdsLib sources directly (no add_subdirectory)"
      contains: "CONFIG_DEFAULT_LOGLEVEL"
    - path: "test_harness/dump_golden.cpp"
      provides: "reference-frame emitter reusing AmsHeader.h structs + Frame::prepend"
    - path: "test/golden/read_device_info_req.hex"
      provides: "byte-exact anchor request frame"
      contains: "000020000000"
  key_links:
    - from: "test_harness/dump_golden.cpp"
      to: "third_party/ADS/AdsLib/AmsHeader.h"
      via: "#include AoEHeader/AmsTcpHeader structs"
      pattern: "AmsHeader\\.h"
    - from: "test_harness/CMakeLists.txt"
      to: "third_party/ADS/AdsLib/Frame.cpp"
      via: "direct source listing in ads_framing target"
      pattern: "Frame\\.cpp"
---

<objective>
Build the self-owned CMake harness and the `dump_golden` tool that reuses the reference AdsLib serialization structs to emit byte-authoritative request/response frames for the six core commands into `test/golden/*.hex`.

Purpose: The golden files are the independent C++ source of truth the Dart codec is validated against. They MUST be generated by code that shares AdsLib's exact `#pragma pack(1)` byte layout — never hand-typed — so parity means real parity.
Output: `test_harness/CMakeLists.txt`, `test_harness/dump_golden.cpp`, and twelve committed `test/golden/*.hex` files (req+res for ReadDeviceInfo, Read, Write, ReadState, WriteControl, ReadWrite).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-protocol-framing-codecs-c-golden-frame-harness/01-CONTEXT.md
@.planning/phases/01-protocol-framing-codecs-c-golden-frame-harness/01-RESEARCH.md

<interfaces>
<!-- Reference structs to reuse verbatim from the vendored submodule. Do NOT re-derive layouts. -->
From third_party/ADS/AdsLib/AmsHeader.h (header-only, #pragma pack(1)):
  AmsTcpHeader   { u16 reserved=0; u32 leLength; }                 // 6 bytes
  AoEHeader      { u8 targetNetId[6]; u16 leTargetPort; u8 sourceNetId[6]; u16 leSourcePort;
                   u16 leCmdId; u16 leStateFlags; u32 leLength; u32 leErrorCode; u32 leInvokeId; } // 32 bytes
  AoERequestHeader        { u32 leGroup; u32 leOffset; u32 leLength; }        // Read/Write req
  AoEReadWriteReqHeader   { AoERequestHeader; u32 leWriteLength; }            // ReadWrite req
  AdsWriteCtrlRequest     { u16 leAdsState; u16 leDevState; u32 leLength; }   // WriteControl req
  AoEResponseHeader       { u32 leResult; }                                   // Write/WriteControl/ReadState/ReadDeviceInfo/... resp
  AoEReadResponseHeader   { AoEResponseHeader; u32 leReadLength; }            // Read/ReadWrite resp
From third_party/ADS/AdsLib/Frame.h: Frame::prepend<T>(const T&); data(); size()
From third_party/ADS/AdsLib/standalone/AdsDef.h: AmsNetId, command IDs (READ_DEVICE_INFO=0x01 ... READ_WRITE=0x09)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Self-owned CMakeLists building ads_framing + dump_golden</name>
  <files>test_harness/CMakeLists.txt</files>
  <read_first>
    - .planning/phases/01-protocol-framing-codecs-c-golden-frame-harness/01-RESEARCH.md (CMake Harness Build Recipe — the VERIFIED recipe and the five critical gotchas)
    - third_party/ADS/AdsLib/CMakeLists.txt (to confirm source locations; do NOT add_subdirectory it)
  </read_first>
  <action>Create test_harness/CMakeLists.txt with `cmake_minimum_required(VERSION 3.16)` and `project(dart_ads_harness CXX)`, `set(CMAKE_CXX_STANDARD 14)` + `CMAKE_CXX_STANDARD_REQUIRED ON`. Define a STATIC library `ads_framing` listing exactly four upstream sources by direct path relative to the repo root — `third_party/ADS/AdsLib/Frame.cpp`, `Log.cpp`, `AdsDef.cpp`, and `standalone/AmsNetId.cpp` (compute the path so it works with `cmake -S test_harness -B test_harness/build`, e.g. via `${CMAKE_SOURCE_DIR}/../third_party/ADS/AdsLib`). Set `target_include_directories(ads_framing PUBLIC ...AdsLib ...AdsLib/standalone)` and `target_compile_definitions(ads_framing PUBLIC CONFIG_DEFAULT_LOGLEVEL=1)` — this define is REQUIRED (Log.cpp references it; a direct compile fails without it). Add `add_executable(dump_golden dump_golden.cpp)` linking `ads_framing`. Do NOT `add_subdirectory(third_party/ADS)` — upstream's root CMake requires 3.23 (`FILE_SET HEADERS`) and drags in sockets/threads. The `mock_server` target is added in plan 01-03; do not reference it here yet.</action>
  <verify>
    <automated>grep -q 'CONFIG_DEFAULT_LOGLEVEL' test_harness/CMakeLists.txt && grep -q 'VERSION 3.16' test_harness/CMakeLists.txt && ! grep -q 'add_subdirectory' test_harness/CMakeLists.txt && echo OK</automated>
  </verify>
  <done>CMakeLists sets a 3.16 floor, defines CONFIG_DEFAULT_LOGLEVEL=1, lists the four AdsLib sources directly, and does not add_subdirectory upstream.</done>
  <acceptance_criteria>
    - Contains `cmake_minimum_required(VERSION 3.16)`
    - Contains `CONFIG_DEFAULT_LOGLEVEL` compile definition
    - References `Frame.cpp`, `Log.cpp`, `AdsDef.cpp`, `AmsNetId.cpp` as direct sources
    - Contains NO `add_subdirectory` directive
    - Defines an `add_executable(dump_golden ...)` target
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: dump_golden.cpp emitting six commands (request + response)</name>
  <files>test_harness/dump_golden.cpp</files>
  <read_first>
    - .planning/phases/01-protocol-framing-codecs-c-golden-frame-harness/01-RESEARCH.md (Verified C++ Header Surface; Per-command byte layouts table; Code Examples → C++ dumper core; verified golden ReadDeviceInfo frame)
    - third_party/ADS/AdsLib/AmsHeader.h (exact struct fields + packing)
    - third_party/ADS/AdsLib/Frame.h (prepend/data/size API)
  </read_first>
  <action>Create test_harness/dump_golden.cpp that builds each frame by reusing upstream structs — NOT by manual memcpy. Follow the verified pattern: construct the ADS payload into a `Frame`, `prepend<AoEHeader>` the 32-byte AMS header, then `prepend<AmsTcpHeader>{ (uint32_t)frame.size() }` so the AMS/TCP length = 32 + payload (the off-by-6/32 pitfall). Bake DETERMINISTIC fixture values so goldens are reproducible: targetNetId 192.168.0.1.1.1 port 851 (AMSPORT_R0_PLC_TC3 / 0x0353), sourceNetId 192.168.0.100.1.1 port 40001 (0x9c41), invokeId 1, stateFlags 0x0004 for requests / 0x0005 for responses. Emit twelve frames covering: ReadDeviceInfo (req: 0 payload; resp: result u32=0 + version u8 + revision u8 + build u16 + name[16]=24B), Read 0x02 (req: group/offset/length 12B; resp: result u32 + readLength u32 + data[readLength]), Write 0x03 (req: group/offset/length 12B + data; resp: result u32 4B), ReadState 0x04 (req: none; resp: result u32 + adsState u16 + deviceState u16 = 8B), WriteControl 0x05 (req: AdsWriteCtrlRequest 8B + data; resp: result u32 4B), ReadWrite 0x09 (req: AoEReadWriteReqHeader 16B + writeData; resp: result u32 + readLength u32 + data). Use small fixed sample data/values for the variable-length payloads (document the chosen values in comments). Write each frame to `test/golden/<name>_req.hex` / `<name>_res.hex` as lowercase hex, one frame per file, with a leading `#`-comment line naming the frame and its fixture params (matching the anchor comment style in RESEARCH). Accept the output directory as argv[1] (default `test/golden/`). Do NOT open sockets or threads.</action>
  <verify>
    <automated>grep -c 'AmsHeader.h\|AoEHeader\|AmsTcpHeader' test_harness/dump_golden.cpp | grep -qv '^0$' && grep -q 'prepend' test_harness/dump_golden.cpp && echo OK</automated>
  </verify>
  <done>dump_golden.cpp reuses AoEHeader/AmsTcpHeader + Frame::prepend, emits req+res hex for all six commands to an argv-supplied directory with deterministic fixture values.</done>
  <acceptance_criteria>
    - Includes `AmsHeader.h` and uses `AoEHeader` + `AmsTcpHeader` (source assertion)
    - Uses `Frame::prepend` for header layering (no manual memcpy of headers)
    - Sets AMS/TCP length via `frame.size()` after prepending the AoEHeader (32 + payload semantics)
    - Emits both `_req.hex` and `_res.hex` for all six commands (12 files) to argv[1]/default dir
    - Response frames use stateFlags 0x0005; requests use 0x0004
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Build the harness, generate and commit goldens, assert the anchor</name>
  <files>test/golden/read_device_info_req.hex, test/golden/read_device_info_res.hex, test/golden/read_req.hex, test/golden/read_res.hex, test/golden/write_req.hex, test/golden/write_res.hex, test/golden/read_state_req.hex, test/golden/read_state_res.hex, test/golden/write_control_req.hex, test/golden/write_control_res.hex, test/golden/read_write_req.hex, test/golden/read_write_res.hex</files>
  <read_first>
    - .planning/phases/01-protocol-framing-codecs-c-golden-frame-harness/01-RESEARCH.md (verified golden ReadDeviceInfo request frame — the exact expected anchor bytes; CMake generator gotcha: no ninja on dev machine, use Unix Makefiles)
  </read_first>
  <action>Configure and build with the default Unix Makefiles generator (dev machine has no ninja): `cmake -S test_harness -B test_harness/build` then `cmake --build test_harness/build`. Run `test_harness/build/dump_golden test/golden/` to emit the twelve `.hex` files. Verify the anchor: the concatenated hex bytes of `test/golden/read_device_info_req.hex` (comments/whitespace stripped) MUST equal `000020000000c0a8000101015303c0a800640101419c01000400000000000000000001000000` (38 bytes; AMS/TCP length 0x20=32). If it differs, the fixture values or length arithmetic are wrong — fix dump_golden before committing. Commit the generated `.hex` files (they are committed so unit tests stay hermetic and need no C++ toolchain).</action>
  <verify>
    <automated>cmake -S test_harness -B test_harness/build && cmake --build test_harness/build && test_harness/build/dump_golden test/golden/ && test "$(sed 's/#.*//' test/golden/read_device_info_req.hex | tr -d '[:space:]')" = "000020000000c0a8000101015303c0a800640101419c01000400000000000000000001000000"</automated>
  </verify>
  <done>Harness builds via CMake+Makefiles; dump_golden produces all twelve committed goldens; the ReadDeviceInfo request golden matches the verified 38-byte anchor byte-for-byte.</done>
  <acceptance_criteria>
    - `cmake --build test_harness/build` exits 0 and produces `test_harness/build/dump_golden`
    - All twelve `test/golden/*.hex` files exist and are non-empty
    - `test/golden/read_device_info_req.hex` decodes to exactly `000020000000c0a8000101015303c0a800640101419c01000400000000000000000001000000`
    - Each response golden's AMS header stateFlags field is 0x0005
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendored C++ source → local build | AdsLib sources compiled into the harness |
| generated bytes → committed fixtures | goldens become the trusted parity oracle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-SC | Tampering | third_party/ADS compiled into ads_framing | mitigate | Compile only the four pinned-commit sources directly (no add_subdirectory, no upstream build system); pin verified in 01-01. Harness excluded from published package via `.pubignore`. |
| T-1-GF | Tampering | test/golden/*.hex authenticity | mitigate | Goldens generated by code reusing AdsLib's own `#pragma pack(1)` structs + Frame::prepend (never hand-typed); anchor asserted against the RESEARCH-verified byte vector before commit. |
</threat_model>

<verification>
- `cmake -S test_harness -B test_harness/build && cmake --build test_harness/build` succeeds on the dev machine (macOS/AppleClang, Unix Makefiles).
- `dump_golden` regenerates identical bytes on re-run (deterministic); anchor equals the verified 38-byte vector.
</verification>

<success_criteria>
The CMake harness builds `dump_golden` from vendored AdsLib sources and emits twelve byte-authoritative golden frames, with the ReadDeviceInfo request matching the verified anchor exactly. Satisfies success criterion #1 (harness online, emits reference vectors) and provides the oracle for TEST-02 parity in plan 01-05.
</success_criteria>

<output>
Create `.planning/phases/01-protocol-framing-codecs-c-golden-frame-harness/01-02-SUMMARY.md` when done
</output>
