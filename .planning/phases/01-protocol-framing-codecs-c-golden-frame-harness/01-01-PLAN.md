---
phase: 01-protocol-framing-codecs-c-golden-frame-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - analysis_options.yaml
  - dart_test.yaml
  - .pubignore
  - .gitmodules
  - third_party/ADS
  - lib/dart_ads.dart
  - test/support/hex.dart
  - test/unit/hex_support_test.dart
autonomous: true
requirements: [TEST-02]
must_haves:
  truths:
    - "dart pub get resolves with zero errors on the pinned SDK floor"
    - "The Beckhoff/ADS reference sources exist on disk at a pinned commit under third_party/ADS"
    - "Tests can turn a '#'-commented hex golden file into a Uint8List"
  artifacts:
    - path: "pubspec.yaml"
      provides: "dart_ads package manifest, SDK floor, dev deps test+lints"
      contains: "name: dart_ads"
    - path: "dart_test.yaml"
      provides: "unit/integration/golden test tags"
      contains: "integration"
    - path: ".pubignore"
      provides: "excludes C++ harness + submodule from published package"
      contains: "test_harness/"
    - path: "third_party/ADS/AdsLib/AmsHeader.h"
      provides: "vendored reference frame structs at pinned commit"
    - path: "test/support/hex.dart"
      provides: "readGolden() hex-fixture parser"
      exports: ["readGolden"]
  key_links:
    - from: "test/unit/hex_support_test.dart"
      to: "test/support/hex.dart"
      via: "import + readGolden call"
      pattern: "readGolden\\("
---

<objective>
Stand up the greenfield `dart_ads` Dart package skeleton, vendor the Beckhoff/ADS reference implementation as a pinned git submodule, and provide the hex-fixture test-support parser that every golden-parity test will depend on.

Purpose: Nothing else in Phase 1 can build or test without a resolvable package, the reference C++ sources on disk, and a way to read committed golden `.hex` files. This is the foundation wave.
Output: A `dart pub get`-clean package, `third_party/ADS` submodule pinned to a verified commit, and `test/support/hex.dart` with a passing self-test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-protocol-framing-codecs-c-golden-frame-harness/01-CONTEXT.md
@.planning/phases/01-protocol-framing-codecs-c-golden-frame-harness/01-RESEARCH.md

<interfaces>
<!-- Hex parser contract downstream golden tests will call. Executor implements exactly this. -->
From test/support/hex.dart (to create):
  Uint8List readGolden(String path)
  - strips inline '#' comments and all whitespace, parses the remaining hex nibble pairs
  - `path` is relative to the package root (e.g. 'test/golden/read_device_info_req.hex')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dart package scaffold (manifest, lints, test tags, pubignore, barrel stub)</name>
  <files>pubspec.yaml, analysis_options.yaml, dart_test.yaml, .pubignore, lib/dart_ads.dart</files>
  <read_first>
    - CLAUDE.md (Technology Stack section — SDK floor, dev-dep versions, endianness rules)
    - .planning/phases/01-protocol-framing-codecs-c-golden-frame-harness/01-RESEARCH.md (Standard Stack, Recommended Project Structure)
    - .planning/phases/01-protocol-framing-codecs-c-golden-frame-harness/01-CONTEXT.md (Repo & Package Layout locked decisions)
  </read_first>
  <action>Create pubspec.yaml with `name: dart_ads`, a short description, `environment: sdk: '>=3.5.0 <4.0.0'` (per locked decision D — Dart SDK floor), and `dev_dependencies: test: ^1.31.0` and `lints: ^6.1.0`. Do NOT add any runtime dependencies — the `protocol/` subtree imports only `dart:typed_data`. Do NOT add a `platforms:` block or `executables:` entry — native-platform declaration and CLI packaging are PKG-01/PKG-02 (Phase 9), out of scope here. Create analysis_options.yaml with `include: package:lints/recommended.yaml`. Create dart_test.yaml declaring three tags — `unit`, `integration`, `golden` — with `integration` given `timeout: 30s`; this lets quick runs use `dart test -x integration`. Create .pubignore excluding `test_harness/`, `third_party/`, and `.planning/` from the published package. Create lib/dart_ads.dart as a minimal barrel: a library doc comment and a `library;` directive only — public exports are finalized in plan 01-07 once all protocol types exist, so leave it export-free for now to keep intermediate waves analyzer-clean. Run `dart pub get` to materialize the lockfile.</action>
  <verify>
    <automated>dart pub get && dart analyze --fatal-infos</automated>
  </verify>
  <done>`dart pub get` resolves with no errors; `dart analyze` is clean; pubspec name is `dart_ads` with SDK floor `>=3.5.0 <4.0.0` and dev deps test+lints only.</done>
  <acceptance_criteria>
    - `pubspec.yaml` contains the exact line `name: dart_ads`
    - `pubspec.yaml` `environment.sdk` is `>=3.5.0 <4.0.0`
    - `pubspec.yaml` has NO `dependencies:` entries (runtime), only `dev_dependencies:` test and lints
    - `analysis_options.yaml` contains `include: package:lints/recommended.yaml`
    - `dart_test.yaml` defines tags `unit`, `integration`, `golden`
    - `.pubignore` contains substrings `test_harness/` and `third_party/`
    - `dart pub get` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Vendor Beckhoff/ADS as a pinned git submodule</name>
  <files>.gitmodules, third_party/ADS</files>
  <read_first>
    - .planning/phases/01-protocol-framing-codecs-c-golden-frame-harness/01-RESEARCH.md (Standard Stack → Submodule pinning; CMake Harness Build Recipe)
  </read_first>
  <action>Add the reference implementation as a git submodule: `git submodule add https://github.com/Beckhoff/ADS.git third_party/ADS`, then pin it by `git -C third_party/ADS checkout 57d63747271fca7881bec48417adb44876e67505` (the commit whose 4-source build recipe was verified hands-on in RESEARCH.md). Stage the resulting `.gitmodules` and the submodule gitlink. If a maintainer prefers a tagged release instead of this exact commit, the 4-source compile (Frame.cpp, Log.cpp, AdsDef.cpp, standalone/AmsNetId.cpp with `-DCONFIG_DEFAULT_LOGLEVEL=1`) MUST be re-verified before changing the pin — do not silently retarget. Confirm the header surface is present after checkout: `third_party/ADS/AdsLib/AmsHeader.h` and `third_party/ADS/AdsLib/standalone/AdsDef.h` must exist.</action>
  <verify>
    <automated>test -f third_party/ADS/AdsLib/AmsHeader.h && test -f third_party/ADS/AdsLib/Frame.cpp && test -f third_party/ADS/AdsLib/standalone/AmsNetId.cpp && grep -q 'third_party/ADS' .gitmodules && echo OK</automated>
  </verify>
  <done>Submodule present at the pinned commit; `.gitmodules` references `third_party/ADS`; the four build-recipe sources and `AmsHeader.h` exist on disk.</done>
  <acceptance_criteria>
    - `third_party/ADS/AdsLib/AmsHeader.h` exists
    - `third_party/ADS/AdsLib/Frame.cpp`, `Log.cpp`, `AdsDef.cpp`, and `standalone/AmsNetId.cpp` all exist
    - `.gitmodules` contains a `path = third_party/ADS` entry
    - `git -C third_party/ADS rev-parse HEAD` returns `57d63747271fca7881bec48417adb44876e67505` (unless an alternate pin was re-verified and documented in the SUMMARY)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Hex-fixture parser + self-test</name>
  <files>test/support/hex.dart, test/unit/hex_support_test.dart</files>
  <read_first>
    - .planning/phases/01-protocol-framing-codecs-c-golden-frame-harness/01-RESEARCH.md (Code Examples → Dart hex fixture parser)
    - test/support/hex.dart (will not exist yet — confirms greenfield)
  </read_first>
  <behavior>
    - Given a string with inline `#` comments and mixed whitespace/newlines, readGolden returns only the decoded bytes
    - `00 20# comment\n0000` parses to bytes [0x00, 0x20, 0x00, 0x00]
    - A fully-commented / whitespace-only file parses to an empty Uint8List
    - The verified anchor `000020000000c0a8000101015303c0a800640101419c01000400000000000000000001000000` round-trips to a 38-byte Uint8List whose first four bytes are 0x00,0x00,0x20,0x00
  </behavior>
  <action>Create test/support/hex.dart exporting `Uint8List readGolden(String path)` in the shape given in RESEARCH.md Code Examples: read the file lines, drop everything after the first `#` on each line, join, strip all whitespace via `RegExp(r'\s')`, then decode nibble pairs with `int.parse(..., radix: 16)` into a `Uint8List`. Import `dart:io` and `dart:typed_data` (this is test-only support, not `lib/`, so `dart:io` is allowed here). Write test/unit/hex_support_test.dart with the four behavior cases above — write a temp `.hex` file to disk (or a fixture under test/support/) for the comment/whitespace cases, and assert the 38-byte anchor decodes with the documented leading bytes. Tag the test group `@Tags(['unit'])`.</action>
  <verify>
    <automated>dart test test/unit/hex_support_test.dart -x integration</automated>
  </verify>
  <done>`readGolden` strips comments/whitespace and returns correct bytes; the 38-byte anchor fixture decodes to the expected leading little-endian bytes; test passes.</done>
  <acceptance_criteria>
    - test/support/hex.dart exports a top-level function `Uint8List readGolden(String path)`
    - Test asserts `readGolden` on the 38-byte anchor yields `length == 38`
    - Test asserts inline `#` comments and whitespace are stripped
    - `dart test test/unit/hex_support_test.dart` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream git remote → repo | Vendored third-party C++ source enters the build |
| pub.dev → dev toolchain | dev_dependencies fetched into the analyzer/test env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-SC | Tampering | third_party/ADS submodule | mitigate | Pin submodule to exact commit `57d63747...`; excluded from published package via `.pubignore`. Re-pin only after re-verifying the 4-source build. |
| T-1-SC | Tampering | dev_dependencies (test, lints) | accept | First-party Dart-team packages (publisher dart.dev), audited Approved in RESEARCH Package Legitimacy Audit; slopcheck unavailable but provenance CITED. No install-time checkpoint required. |
</threat_model>

<verification>
- `dart pub get` clean, `dart analyze --fatal-infos` clean, `dart test test/unit/hex_support_test.dart` green.
- Submodule present at pinned commit with the four build-recipe sources on disk.
</verification>

<success_criteria>
Package resolves, reference sources vendored at a pinned commit, and the golden-hex parser is proven by a passing self-test. Downstream waves can now build the codec (04) and the C++ harness (02) against a stable base.
</success_criteria>

<output>
Create `.planning/phases/01-protocol-framing-codecs-c-golden-frame-harness/01-01-SUMMARY.md` when done
</output>
