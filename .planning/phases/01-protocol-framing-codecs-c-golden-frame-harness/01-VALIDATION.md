---
phase: 1
slug: protocol-framing-codecs-c-golden-frame-harness
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-03
---

# Phase 1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | dart test (package:test ^1.31.0) |
| **Config file** | none — Wave 0 installs (`pubspec.yaml`, `dart_test.yaml` with `unit`/`integration` tags) |
| **Quick run command** | `dart test -x integration` |
| **Full suite command** | `dart test` (requires golden files generated by the CMake harness) |
| **Estimated runtime** | ~10 seconds (unit); ~60 seconds incl. CMake harness build on first run |
| **C++ harness build** | `cmake -S test_harness -B test_harness/build && cmake --build test_harness/build` |
| **Golden generation** | `test_harness/build/dump_golden test/golden/` |

---

## Sampling Rate

- **After every task commit:** Run `dart test -x integration`
- **After every plan wave:** Run full suite: build harness (if C++ changed), regenerate goldens, `dart test`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| (filled by planner) | — | — | PROTO-01 | T-1-01 | Length fields validated before allocation | unit | `dart test test/protocol/` | ❌ W0 | ⬜ pending |
| (filled by planner) | — | — | PROTO-02 | T-1-01 | Max-frame guard rejects oversized frames | unit | `dart test test/protocol/frame_assembler_test.dart` | ❌ W0 | ⬜ pending |
| (filled by planner) | — | — | TEST-01 | — | N/A | build | `cmake --build test_harness/build` | ❌ W0 | ⬜ pending |
| (filled by planner) | — | — | TEST-02 | — | N/A | unit | `dart test -t golden` | ❌ W0 | ⬜ pending |
| (filled by planner) | — | — | TEST-04 | — | N/A | unit | `dart test test/protocol/frame_assembler_test.dart` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `pubspec.yaml` + `analysis_options.yaml` — Dart package scaffold (`dart_ads`, SDK >=3.5.0 <4.0.0, dev_deps: test, lints)
- [ ] `dart_test.yaml` — `unit`/`integration`/`golden` tags so quick runs exclude harness-dependent tests
- [ ] `test_harness/CMakeLists.txt` + vendored submodule — C++ toolchain for golden generation
- [ ] `test/golden/` — directory for reference frames (generated, committed for hermetic unit tests)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| CI green on Linux (GitHub Actions harness job) | TEST-01 | Requires push to GitHub; not locally verifiable | Push branch, confirm both workflow jobs pass |

*All other phase behaviors have automated verification.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
