---
phase: 05-device-notifications-as-streams
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - test_harness/dump_golden.cpp
  - test/golden/add_notification_req.hex
  - test/golden/add_notification_res.hex
  - test/golden/del_notification_req.hex
  - test/golden/del_notification_res.hex
  - test/golden/notification_stream.hex
  - test/unit/golden_parity_test.dart
autonomous: true
requirements: [NOTIF-01, NOTIF-03]
must_haves:
  truths:
    - "dump_golden emits byte-authoritative fixtures for AddDeviceNotification req/res, DeleteDeviceNotification req/res, and a 2-stamp x 2-sample notification stream"
    - "buildAddNotificationPayload output matches add_notification_req.hex byte-for-byte (40-byte layout, field order, reserved zeros)"
    - "decodeAddNotificationResponse against add_notification_res.hex yields the golden result + handle"
    - "parseNotificationStream against notification_stream.hex yields 4 notifications with the golden timestamps/handles/data"
    - "The golden-reproducibility gate holds: re-running dump_golden leaves the committed .hex untouched"
  artifacts:
    - path: "test_harness/dump_golden.cpp"
      provides: "Notification golden emitters (Add/Del req+res, nested 2x2 stream)"
      contains: "add_notification_req"
    - path: "test/golden/notification_stream.hex"
      provides: "Committed 2x2 nested notification frame fixture"
    - path: "test/unit/golden_parity_test.dart"
      provides: "Byte-for-byte parity assertions for the notification builders/decoders/parser"
      contains: "notification"
  key_links:
    - from: "test/unit/golden_parity_test.dart"
      to: "lib/src/protocol/notifications.dart"
      via: "buildAddNotificationPayload / parseNotificationStream under test"
      pattern: "buildAddNotificationPayload"
---

<objective>
Emit and commit the notification golden fixtures from the C++ reference structs, and
assert byte-for-byte parity between those goldens and the pure Dart builders/decoders/parser
from 05-01.

Purpose: pin the notification wire layouts against the vendored Beckhoff C++ the same way every
core command is pinned — the golden reproducibility gate (`dump_golden && git diff --exit-code`)
guarantees the Dart codec cannot silently diverge, and the 2x2 stream golden proves the nested
parser against a C++-produced frame (not just a hand-built Dart one).

Output: edited `dump_golden.cpp`, five committed `.hex` fixtures, extended `golden_parity_test.dart`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-device-notifications-as-streams/05-RESEARCH.md

<interfaces>
<!-- dump_golden.cpp + golden_parity_test.dart idioms to reuse. -->
- dump_golden helpers: `putU16/putU32(vec,x)`, `payloadFrame(vec)`, `wrap(Frame&, cmdId, isResponse)`,
  `writeHex(dir, name, comment, bytes)` (returns bool; accumulate into `ok`).
- Response addressing inverts request addressing (handled inside `wrap(..., isResponse=true)`).
- AoEHeader cmd constants: ADD_DEVICE_NOTIFICATION 0x06, DEL_DEVICE_NOTIFICATION 0x07, DEVICE_NOTIFICATION 0x08.
- Add a `putU64(vec,x)` in dump_golden (8-byte LE) — dump_golden has no such helper yet.
- golden_parity_test.dart: `_adsResponsePayload(frame, expectedCommandId)` strips the 38-byte prefix
  and validates wrapper/addressing; `hexFile(...)` loads a fixture; `_u32le(v)` builds a 4-byte blob.
- From 05-01 (imported): buildAddNotificationPayload, buildDeleteNotificationPayload,
  decodeAddNotificationResponse, decodeDeleteNotificationResponse, parseNotificationStream, AdsTransmissionMode.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Emit the five notification goldens from dump_golden.cpp</name>
  <files>test_harness/dump_golden.cpp, test/golden/*.hex</files>
  <read_first>
    - test_harness/dump_golden.cpp (full — mirror the Read/Write fixture blocks and `wrap`/`writeHex` idiom)
    - .planning/phases/05-device-notifications-as-streams/05-RESEARCH.md "Wire Layouts 1-4"
  </read_first>
  <action>
    Add a `putU64` helper to dump_golden.cpp. Add five fixture blocks, each `ok &= writeHex(...)`:
    (1) `add_notification_req` — a 40-byte payload built as explicit LE scalars in the SAME field
    order as buildAddNotificationPayload: group 0x4020, offset 4, cbLength 1,
    nTransMode 3 (SERVERCYCLE), nMaxDelay 0, nCycleTime 1000000, then 16 zero bytes — wrapped
    `wrap(f, ADD_DEVICE_NOTIFICATION, false)`. Use the exact C++ parity attribs from
    05-RESEARCH ({1, SERVERCYCLE, 0, 1000000}) so this golden doubles as the parity anchor.
    (2) `add_notification_res` — `putU32(0); putU32(0x0A0B0C0D)` (result 0 + a distinctive handle),
    `wrap(..., true)`. (3) `del_notification_req` — `putU32(0x0A0B0C0D)` (handle), `wrap(false)`.
    (4) `del_notification_res` — `putU32(0)`, `wrap(true)`. (5) `notification_stream` — a nested
    payload: `length` (backfilled), `stamps=2`; stamp0 {timestamp = a whole-us FILETIME e.g.
    132000000000000000, sampleCount=2, samples (handle 1, size 4, data 11 22 33 44) and
    (handle 2, size 2, data aa bb)}; stamp1 {timestamp = another whole-us FILETIME, sampleCount=2,
    samples (handle 1, size 1, data 55) and (handle 3, size 0, no data)}; `wrap(f,
    DEVICE_NOTIFICATION, true)`. Backfill the leading `length` = bytes after it. Distinct
    timestamps per stamp and distinct handles/sizes/data prove the nested loop.
  </action>
  <verify>
    <automated>cmake --build test_harness/build --target dump_golden 2>&1 | tail -3 && test_harness/build/dump_golden test/golden && test -f test/golden/notification_stream.hex && git diff --exit-code test/golden/*.hex && echo "goldens reproducible"</automated>
  </verify>
  <done>Five .hex fixtures exist and are byte-reproducible (a second dump_golden run leaves them git-clean).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Byte-for-byte parity assertions against the goldens</name>
  <files>test/unit/golden_parity_test.dart</files>
  <read_first>
    - test/unit/golden_parity_test.dart (mirror the Read/Write parity groups + `_adsResponsePayload`)
  </read_first>
  <behavior>
    - buildAddNotificationPayload(group 0x4020, offset 4, length 1, transMode serverCycle.code,
      maxDelay100ns 0, cycleTime100ns 1000000) equals the ADS payload extracted from
      add_notification_req.hex, byte-for-byte.
    - buildDeleteNotificationPayload(handle 0x0A0B0C0D) equals del_notification_req's payload.
    - decodeAddNotificationResponse(add_notification_res payload) == (result: 0, handle: 0x0A0B0C0D).
    - decodeDeleteNotificationResponse(del_notification_res payload) == 0.
    - parseNotificationStream(notification_stream payload) yields 4 notifications matching the golden
      timestamps (per-stamp), handles, sizes, and data.
  </behavior>
  <action>
    Add a `group('device notification goldens', ...)` to golden_parity_test.dart. Import
    `package:dart_ads/src/protocol/notifications.dart`. For each req/res golden, load via the
    existing hex loader, strip the 38-byte prefix with `_adsResponsePayload(frame, expectedCommandId)`
    (or the request-side equivalent used for req goldens), and `expect` equality against the 05-01
    builder/decoder output. For the stream golden, extract the 0x08 payload and assert
    `parseNotificationStream(...)` returns the 4 expected AdsNotification values (compare handle,
    timestamp, and data). Keep the existing groups untouched.
  </action>
  <verify>
    <automated>dart test test/unit/golden_parity_test.dart -n "notification"</automated>
  </verify>
  <done>All notification golden-parity assertions pass; the full golden_parity_test.dart file stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| C++ reference bytes -> Dart codec | Goldens are the trusted oracle; the Dart codec is the artifact under test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-05 | Tampering | Add-request field order / reserved bytes drift | mitigate | Golden pins the exact 40-byte layout; a transposition or missing reserved byte fails the parity test |
| T-5-09 | Tampering | Silent golden non-reproduction (I/O false-pass) | accept | writeHex returns bool aggregated into a non-zero exit; the `git diff --exit-code` gate catches drift |
</threat_model>

<verification>
- `test_harness/build/dump_golden test/golden` exits 0 and leaves `git diff --exit-code test/golden/*.hex` clean.
- `dart test test/unit/golden_parity_test.dart` green (existing + notification groups).
</verification>

<success_criteria>
- Five committed, reproducible notification goldens.
- Dart builders/decoders/parser proven byte-for-byte against C++-produced frames, including the 2x2 nested stream.
</success_criteria>

<output>
Create `.planning/phases/05-device-notifications-as-streams/05-03-SUMMARY.md` when done.
</output>
