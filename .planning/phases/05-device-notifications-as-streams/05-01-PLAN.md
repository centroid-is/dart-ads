---
phase: 05-device-notifications-as-streams
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/src/protocol/notifications.dart
  - test/unit/protocol/notifications_test.dart
autonomous: true
requirements: [NOTIF-01, NOTIF-03, NOTIF-04]
must_haves:
  truths:
    - "A 40-byte AddDeviceNotification payload is built with the exact field order indexGroup, indexOffset, cbLength, nTransMode, nMaxDelay, nCycleTime + 16 reserved zero bytes"
    - "A nested 0x08 stream with 2 stamps x 2 samples parses into 4 AdsNotification values with the correct per-stamp timestamps and per-sample handles/data"
    - "A FILETIME (100ns ticks since 1601) converts to a UTC DateTime; a whole-microsecond FILETIME round-trips exactly"
    - "A truncated / oversized / length-mismatched 0x08 payload throws MalformedFrameException, never reads out of bounds"
    - "AdsTransmissionMode exposes noTrans/clientCycle/clientOnChange/serverCycle/serverOnChange (+2/2 variants + client1Req) with their ADSTRANSMODE wire codes"
  artifacts:
    - path: "lib/src/protocol/notifications.dart"
      provides: "Pure notification protocol: AdsTransmissionMode, AdsNotification, FILETIME helpers, Add/Delete payload builders + response decoders, parseNotificationStream"
      contains: "parseNotificationStream"
      min_lines: 120
    - path: "test/unit/protocol/notifications_test.dart"
      provides: "Unit coverage: builders, decoders, FILETIME round-trip/truncation, nested 2x2 parse, overrun rejection"
      contains: "parseNotificationStream"
  key_links:
    - from: "lib/src/protocol/notifications.dart"
      to: "lib/src/protocol/range_check.dart"
      via: "checkUint on every u32 payload field"
      pattern: "checkUint"
    - from: "parseNotificationStream"
      to: "filetimeToDateTime"
      via: "per-stamp timestamp conversion"
      pattern: "filetimeToDateTime"
---

<objective>
Create the pure, golden-testable notification protocol layer: the `AdsTransmissionMode`
enum, the `AdsNotification` value type, the FILETIME<->DateTime helpers, the
AddDeviceNotification / DeleteDeviceNotification payload builders and response
decoders, and the doubly-nested `parseNotificationStream`.

Purpose: this file is the single source of truth for every notification wire layout.
The connection layer (05-04), the client (05-05), the golden fixtures (05-03), and the
mock (05-02) all agree with it by construction. It is pure (`dart:typed_data` only —
no `dart:async`/`dart:io`) so it is unit- and golden-testable with no socket.

Output: `lib/src/protocol/notifications.dart` + `test/unit/protocol/notifications_test.dart`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-device-notifications-as-streams/05-RESEARCH.md
@.planning/phases/05-device-notifications-as-streams/05-CONTEXT.md

<interfaces>
<!-- Established patterns this file MUST mirror. Do not re-explore the codebase. -->

From lib/src/protocol/commands.dart (builder pattern to copy exactly):
```dart
Uint8List buildReadPayload({required int indexGroup, required int indexOffset, required int length}) {
  final payload = Uint8List(12);
  final bd = ByteData.sublistView(payload);
  bd.setUint32(0, checkUint(indexGroup, 32, 'indexGroup'), Endian.little);
  // ... setUint32 for each field ...
  return payload;
}
// Decoders use `_require(payload, min, what)` to guard length before reading.
```

From lib/src/protocol/range_check.dart:
```dart
int checkUint(int value, int bits, String name); // throws ArgumentError if out of range
```

From lib/src/protocol/exceptions.dart:
```dart
class MalformedFrameException implements Exception {
  const MalformedFrameException(this.message, {this.length, this.offset});
}
```

From lib/src/protocol/constants.dart:
```dart
abstract final class AdsCommandId {
  static const int addDeviceNotification = 0x06;
  static const int deleteDeviceNotification = 0x07;
  static const int deviceNotification = 0x08;
}
```
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Enum, value type, FILETIME helpers, payload builders + response decoders</name>
  <files>lib/src/protocol/notifications.dart, test/unit/protocol/notifications_test.dart</files>
  <read_first>
    - lib/src/protocol/commands.dart (builder + `_require` decoder pattern to mirror)
    - lib/src/protocol/range_check.dart (checkUint signature)
    - .planning/phases/05-device-notifications-as-streams/05-RESEARCH.md sections "Wire Layouts 1/2/3" and "FILETIME -> DateTime Conversion"
  </read_first>
  <behavior>
    - buildAddNotificationPayload returns exactly 40 bytes; bytes at offsets 0/4/8/12/16/20 are indexGroup/indexOffset/cbLength(length)/nTransMode/nMaxDelay/nCycleTime LE; bytes 24..39 are zero.
    - buildDeleteNotificationPayload(handle: h) returns 4 bytes = h LE.
    - decodeAddNotificationResponse: on result==0 requires >=8 bytes and returns (result, handle) reading handle at offset 4; on non-zero result tolerates a 4-byte payload (handle absent) — mirror the commands.dart "check result before reading data" guard.
    - decodeDeleteNotificationResponse requires >=4 bytes, returns result u32.
    - filetimeToDateTime(116444736000000000) == DateTime.utc(1970,1,1); a whole-microsecond FILETIME (multiple of 10) round-trips exactly through dateTimeToFiletime; a FILETIME with a non-zero 100ns digit truncates predictably (~/10).
    - AdsTransmissionMode.serverOnChange.code == 4; every member exposes its ADSTRANSMODE code.
    - checkUint rejects an out-of-u32 maxDelay/cycleTime (ArgumentError).
  </behavior>
  <action>
    Create `lib/src/protocol/notifications.dart` as a pure library (import only
    `dart:typed_data`, plus local `range_check.dart`, `exceptions.dart`, `constants.dart`).
    Define: (1) `enum AdsTransmissionMode` with members noTrans(0), clientCycle(1),
    clientOnChange(2), serverCycle(3), serverOnChange(4), serverCycle2(5),
    serverOnChange2(6), client1Req(10), each carrying a final `int code`; (2) an
    `AdsNotification` value class with `int handle`, `DateTime timestamp`, `Uint8List data`
    and a `toString`; (3) `const int _filetimeEpochOffset = 116444736000000000;` plus
    `DateTime filetimeToDateTime(int filetime)` (`(filetime - offset) ~/ 10` micros,
    `DateTime.fromMicrosecondsSinceEpoch(..., isUtc: true)`) and `int dateTimeToFiletime(DateTime dt)`
    (`dt.toUtc().microsecondsSinceEpoch * 10 + offset`); (4) `buildAddNotificationPayload`
    (40-byte layout per the interface block, `checkUint(..., 32, ...)` on every field, bytes
    24..39 left zero) and `buildDeleteNotificationPayload({required int handle})` (4 bytes);
    (5) `decodeAddNotificationResponse` returning a record `({int result, int handle})` and
    `decodeDeleteNotificationResponse` returning `int result`, both using a local
    `_require`-style length guard copied from commands.dart. Do NOT put the 16 reserved bytes
    behind a flag or omit them (the off-by-16 bug — research Pitfall 4). Use `getUint64`
    (unsigned), never `getInt64`.
  </action>
  <verify>
    <automated>dart analyze --fatal-infos lib/src/protocol/notifications.dart && dart test test/unit/protocol/notifications_test.dart -n "filetime|payload|decode|transmission"</automated>
  </verify>
  <done>Enum, value type, FILETIME helpers, builders and decoders exist and pass their unit tests; analyzer clean with --fatal-infos.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: parseNotificationStream (doubly-nested, bounds-checked)</name>
  <files>lib/src/protocol/notifications.dart, test/unit/protocol/notifications_test.dart</files>
  <read_first>
    - .planning/phases/05-device-notifications-as-streams/05-RESEARCH.md section "Code Examples -> Reading the nested stream" and "Wire Layouts 4"
    - lib/src/protocol/commands.dart (`_decodeResultAndData` overrun-guard idiom to mirror)
  </read_first>
  <behavior>
    - A payload with length u32 (== payload.length-4), stamps u32, then per stamp {timestamp u64, sampleCount u32, per sample {handle u32, size u32, data[size]}} of 2 stamps x 2 samples yields 4 AdsNotification: samples in stamp 0 share stamp-0's timestamp, samples in stamp 1 share stamp-1's; each carries its own handle/size/data (defensive copy, does not alias the input).
    - A single 1x1 frame yields 1 notification (sanity).
    - payload.length < 8 -> MalformedFrameException.
    - length + 4 != payload.length -> MalformedFrameException.
    - a stamp header that overruns, or a sample whose size exceeds remaining bytes -> MalformedFrameException (never reads past the buffer).
    - stamps == 0 yields an empty list.
  </behavior>
  <action>
    Add `List<AdsNotification> parseNotificationStream(Uint8List payload)` transcribed from
    NotificationDispatcher.cpp:56 (do NOT read the C++ leading `fullLength` u32 — it is not on
    the wire; the Dart payload begins at `length`). Validate `payload.length >= 8`,
    `length + 4 == payload.length`, then loop `stamps` outer x `sampleCount` inner. Bounds-check
    BEFORE every read: `off + 12 > payload.length` (stamp header), `off + 8 > payload.length`
    (sample header), `off + size > payload.length` (sample data) — throw MalformedFrameException
    with offset on any overrun. Convert each stamp timestamp via `filetimeToDateTime`. Copy each
    sample's data defensively (`Uint8List.fromList(payload.sublist(...))`). Never mutate a shared
    buffer. This is the anti-flat-parser guard: the nested loop MUST be proven by the 2x2 test.
  </action>
  <verify>
    <automated>dart test test/unit/protocol/notifications_test.dart -n "parse|nested|overrun|stamp|sample"</automated>
  </verify>
  <done>parseNotificationStream returns 4 notifications for the 2x2 fixture, rejects every truncated/oversized/mismatched frame with MalformedFrameException, and the full notifications_test.dart file is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PLC/peer -> parseNotificationStream | Untrusted inbound 0x08 bytes cross into the pure parser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-03 | Tampering | parseNotificationStream sample `size` field | mitigate | Bounds-check `off + size <= payload.length` before slicing; MalformedFrameException on overrun (ASVS V5) |
| T-5-04 | DoS | parseNotificationStream `stamps`/`sampleCount` counts | mitigate | Every header read guarded against remaining length before dereference; a lying count hits an overrun guard, never an unbounded read |
| T-5-05 | Tampering | AddDeviceNotification payload field order / reserved bytes | mitigate | 40-byte layout transcribed from AmsHeader.h:92; reserved 16 bytes always present; pinned by golden in 05-03 |
| T-5-06 | Tampering | FILETIME sign / precision | mitigate | `getUint64` (unsigned); documented 100ns->us truncation; round-trip test only on multiple-of-10 FILETIMEs |
</threat_model>

<verification>
- `dart analyze --fatal-infos lib/src/protocol/notifications.dart` clean.
- `dart test test/unit/protocol/notifications_test.dart` green (parser, FILETIME, builders, decoders).
- File stays pure: `grep -nE "dart:async|dart:io" lib/src/protocol/notifications.dart` returns nothing.
</verification>

<success_criteria>
- 40-byte Add payload, 4-byte Delete payload, both decoders, FILETIME helpers, and the nested parser exist and are unit-proven (incl. the mandatory 2x2 nesting and overrun rejection).
- No `dart:async`/`dart:io` import (purity gate).
</success_criteria>

<output>
Create `.planning/phases/05-device-notifications-as-streams/05-01-SUMMARY.md` when done.
</output>
