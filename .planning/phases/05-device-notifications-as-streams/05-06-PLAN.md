---
phase: 05-device-notifications-as-streams
plan: 06
type: execute
wave: 4
depends_on: ["05-02", "05-04", "05-05"]
files_modified:
  - dart_test.yaml
  - test/integration/ads_notification_test.dart
  - test/integration/notification_parity_test.dart
autonomous: true
requirements: [NOTIF-02, NOTIF-04, TEST-05]
must_haves:
  truths:
    - "dart_test.yaml declares a `slow` tag so testEndurance is excluded by default"
    - "testAdsNotification: subscribe against a mock-known (group,offset), receive >=1 notification, cancel -> Delete, verify no further delivery"
    - "testManyNotifications: 64+ concurrent subscriptions all receive; after cancel/disconnect the mock active-handle count (magic read group 0xE7700002) returns to 0 (deterministic leak proof)"
    - "A hostile 0x08 frame (via burst/crafted trigger) is dropped and the connection stays alive to deliver a subsequent good notification"
    - "serverOnChange vs serverCycle transmission modes both deliver via the live mock"
    - "testEndurance is tagged `slow` and skipped by the default suite"
  artifacts:
    - path: "test/integration/ads_notification_test.dart"
      provides: "Lifecycle (testAdsNotification), first-listen race, hostile-frame, transmission-mode integration"
      contains: "testAdsNotification"
    - path: "test/integration/notification_parity_test.dart"
      provides: "testManyNotifications leak proof + testEndurance (slow)"
      contains: "testManyNotifications"
    - path: "dart_test.yaml"
      provides: "`slow` tag declaration"
      contains: "slow"
  key_links:
    - from: "test/integration/notification_parity_test.dart"
      to: "mock magic read group 0xE7700002"
      via: "AdsClient.read of the active-handle count"
      pattern: "0xE7700002|E7700002"
    - from: "test/integration/ads_notification_test.dart"
      to: "AdsClient.subscribe"
      via: "live stream over the mock"
      pattern: "subscribe"
---

<objective>
Prove the full notification stack against the live C++ mock: the lifecycle (Add on first listen,
receive, cancel->Delete, no further delivery), the deterministic handle-leak proof, transmission
modes, hostile-frame survival, and the C++-named parity ports — with the endurance test tagged
`slow` and excluded by default.

Purpose: this is the phase gate. It exercises the real socket path end-to-end and mechanically ports
the applicable AdsLibTest notification scenarios (TEST-05 slice), asserting the leak count returns
to 0 in-band rather than "probably fine".

Output: `dart_test.yaml` (+`slow` tag), `test/integration/ads_notification_test.dart`,
`test/integration/notification_parity_test.dart`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-device-notifications-as-streams/05-RESEARCH.md
@.planning/phases/05-device-notifications-as-streams/05-CONTEXT.md
@.planning/phases/05-device-notifications-as-streams/05-VALIDATION.md

<interfaces>
<!-- Test harness idioms to reuse (do not re-explore). -->
- `startMockServer({List<String> args})` from test/support/mock_server.dart -> MockServer{proc, port};
  `server.stop()` in tearDown. Pass `['--notify-burst', '3']` to exercise the race/hostile paths.
- Parity-test connect helper (ads_parity_test.dart lines 71-92): build AmsConnection(SocketTransport(),
  source/target AmsAddr), `conn.connect('127.0.0.1', server.port)`, wrap in AdsClient. Copy this
  `connectClient()` shape (fresh mock + connection + client per group; addTearDown for both).
- Group naming: name each `group(...)` EXACTLY after its C++ counterpart (testAdsNotification,
  testManyNotifications, testEndurance) so the Phase-9 audit greps mechanically — mirror the header
  comment style in ads_parity_test.dart.
- Mock magic groups (from 05-02): READ 0xE7700002 -> active-handle count u32; WRITE 0xE7700003 ->
  emit one 2x2 frame. Write-triggered emission fires on a Write to a subscribed (group,offset).
  The C++ parity attribs: group 0x4020, offset 4, cbLength 1, SERVERCYCLE, maxDelay 0, cycle 1000000.
- Standing rules: `@Tags([...])` at top of file; `-n` regex for name filters (never `-N`); every test
  carries a generous per-request timeout so a failure is provably a result, not a timeout.
- AdsClient.subscribe(indexGroup:, indexOffset:, length:, mode:, cycleTime:, maxDelay:) from 05-05.
- conn.droppedNotifications / conn.isConnected are observable for the hostile-frame assertion.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: `slow` tag + testAdsNotification, first-listen-race, hostile-frame, transmission modes</name>
  <files>dart_test.yaml, test/integration/ads_notification_test.dart</files>
  <read_first>
    - dart_test.yaml (tag block)
    - test/integration/ads_parity_test.dart (connectClient helper + group/header style)
    - .planning/phases/05-device-notifications-as-streams/05-RESEARCH.md "C++ Test Parity" (testAdsNotification behaviour)
  </read_first>
  <action>
    (1) In `dart_test.yaml`, add a `slow:` entry under `tags:` (declared so the default run excludes it;
    document it excludes long-running endurance). (2) Create `test/integration/ads_notification_test.dart`
    with `@Tags(['integration'])` and a `connectClient()` helper copied from ads_parity_test.dart.
    Add groups: `group('testAdsNotification', ...)` — subscribe to (0x4020, 4, length 1) mode
    serverOnChange, listen, `client.write` to (0x4020, 4) to trigger emission, assert the stream yields
    >=1 AdsNotification with a UTC timestamp and the written data; cancel the subscription, write again,
    assert NO further notification arrives (bounded wait). `group('first-listen race', ...)` — start the
    mock with `--notify-burst 3`, subscribe+listen, assert the first burst notification is delivered
    (proves synchronous registration; would flake/drop pre-fix). `group('hostile notification frame', ...)`
    — trigger a malformed 0x08 (a crafted burst against a handle plus a deliberately bad frame path, or
    reuse a mock hook) then a good write; assert `conn.droppedNotifications >= 1`, `conn.isConnected`
    true, and the good notification still delivered. `group('transmission modes', ...)` — subscribe once
    with serverOnChange and once with serverCycle (cycleTime 100ms), assert both deliver.
    Use a per-test timeout and StreamQueue/`.first.timeout(...)` for bounded waits (never an unbounded await).
  </action>
  <verify>
    <automated>dart test -t integration test/integration/ads_notification_test.dart -n "testAdsNotification|race|hostile|transmission"</automated>
  </verify>
  <done>`slow` tag declared; lifecycle, race, hostile-frame survival, and both transmission modes pass against the live mock.</done>
</task>

<task type="auto">
  <name>Task 2: testManyNotifications leak proof + testEndurance (slow)</name>
  <files>test/integration/notification_parity_test.dart</files>
  <read_first>
    - .planning/phases/05-device-notifications-as-streams/05-RESEARCH.md "C++ Test Parity" (testManyNotifications, testEndurance) + "Mock active-handle-count exposure"
    - test/integration/ads_parity_test.dart (parallel-operation group style)
  </read_first>
  <action>
    Create `test/integration/notification_parity_test.dart` with `@Tags(['integration'])`.
    `group('testManyNotifications', ...)`: read the active-handle count via `client.read(indexGroup:
    0xE7700002, indexOffset: 0, length: 4)` and assert it decodes to 0; open 64+ concurrent
    subscriptions (distinct offsets), listen to each, trigger a write per subscription and assert every
    stream receives; read the count again and assert == number of active handles; cancel all (and/or
    close the connection) and assert the count returns to 0 — the deterministic leak proof (NOTIF-02).
    Add a `group('testEndurance', ...)` with `@Tags(['integration', 'slow'])` at the test level (or a
    `test('...', tags: 'slow')`): a longer-running register/receive/cancel loop, so it is EXCLUDED by the
    default suite and runnable via `dart test -t slow`. Include the standard header comment mapping each
    group to its `third_party/ADS/AdsLibTest/main.cpp` counterpart (testManyNotifications L998,
    testEndurance L1038) with the CONTEXT adaptation notes (many->64+, throughput->leak-proof, endurance->slow).
  </action>
  <verify>
    <automated>dart test -t integration test/integration/notification_parity_test.dart -n "testManyNotifications" && grep -q "slow" test/integration/notification_parity_test.dart && grep -q "testEndurance" test/integration/notification_parity_test.dart</automated>
  </verify>
  <done>testManyNotifications proves handle count returns to 0 after cancel/disconnect; testEndurance is tagged `slow` (excluded by default, runs under -t slow).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live mock 0x08 stream -> AdsClient stream | End-to-end untrusted notification bytes over a real socket |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-01 | DoS | PLC-side notification handle leak | mitigate | In-band active-handle-count (0xE7700002) asserted 0 before, N during, 0 after cancel/disconnect — deterministic, not "probably fine" |
| T-5-02 | DoS | Hostile 0x08 frame killing all subscriptions | mitigate | `hostile notification frame` group asserts droppedNotifications++ AND connection alive AND later good frame delivered |
| T-5-11 | DoS | First notification dropped (race) | mitigate | `--notify-burst` group asserts the first burst sample is delivered (synchronous registration proof) |
</threat_model>

<verification>
- `dart test -t integration test/integration/ads_notification_test.dart test/integration/notification_parity_test.dart` green (excludes slow).
- `dart test -t slow -n testEndurance` runs and passes manually.
- Default suite excludes slow: `dart test -x slow` green.
- Tag hygiene: `grep -c "slow" dart_test.yaml` (excluding comments) >= 1.
</verification>

<success_criteria>
- testAdsNotification, testManyNotifications ported and green; leak count provably returns to 0.
- Hostile frame survived; first-listen race covered; both transmission modes deliver.
- testEndurance tagged `slow` and excluded by default.
</success_criteria>

<output>
Create `.planning/phases/05-device-notifications-as-streams/05-06-SUMMARY.md` when done.
</output>
